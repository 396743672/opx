# opx 0.5.0 发行说明

发布日期：2026-09-18

> 本版本是 roadmap-2 收官后的一轮大扩展：新增**网站证书自动化（ACME）**、**动态域名（DDNS）**、**DNS 账号独立管理**三条主线，配套**操作记录（审计）二期**、**指标历史持久化与告警通知**、**崩溃自愈（看门狗）**、**首页瘦身 + 监控中心**，并完成设置页 Tab 分区与 Nacos 集群模式。

---

## 新增功能

### 网站证书自动化（ACME DNS-01）
- **一键签发 Let's Encrypt 证书**：站点对话框新增「证书来源」选择（自签 / Let's Encrypt），走 DNS-01 验证，保存站点即完成签发，全程弹窗展示 ACME 阶段进度（10/35/65/90/100%）。
- **自动续期**：每小时检查一次，距到期不足 30 天自动续期；续期按实际结果记审计（reload 失败记为失败，不再无条件记成功）。
- **多 DNS 服务商**（v1 支持 Cloudflare，trait 已预留扩展）：签发与续期按站点绑定的 DNS 账号取凭证。
- **staging 开关**：设置页「域名」Tab 可切换 Let's Encrypt staging 环境（警示态 UI，签发结果明确标注证书不被信任），用于调试避免触发生产速率限制。
- 证书路径统一存相对路径（`sites-data/certs/<域名>.{crt,key}`），nginx 重装后不再失效。

### 动态域名（DDNS）
- **四家服务商**：Cloudflare、阿里云、DNSPod（腾讯云）、华为云，各自的 provider 选择与凭证配置**与网站证书完全解耦**（证书可以是其他家的）。
- **多源公网 IP 检测**：IPv4 / IPv6 分家族专用端点，逐个尝试并汇总全部失败原因。
- **5 分钟调度器** + 设置页「立即同步」按钮；`sync_once` 串行化，避免调度器与手动同步并发创建重复记录。
- 设置页 DDNS 区块：四家凭证切换 + 域名列表 + 手动同步。

### DNS 账号独立管理
- **新增「DNS 账号」页面**：集中管理各家 DNS 服务商凭证（名称 / 服务商 / 凭证 / zone 缓存 / 测试时间），支持「测试连通」真实拉取 zone 列表。
- **站点绑定账号**：站点 SSL 配置改为下拉选择 DNS 账号，不同站点可用不同服务商；站点域名支持从账号缓存的 zone 辅助填充。
- **旧配置自动迁移**：启动时检测旧全局 Cloudflare Token，自动生成「默认 Cloudflare」账号并绑定已勾 ACME 的站点（幂等，重启不重复迁移）。
- **引用保护**：删除被站点引用的账号会被拦截并提示站点名。
- 设置页移除全局 DNS 凭证区块（signature 层零改动，仅 `DnsProvider` trait 抽象重组）。

### 操作记录（审计日志）
- **可视化页面**：侧边栏新增「操作记录」，支持概览（今日 / 总数 / 按操作计数 / 最近时间）、按天 / 操作类型 / 关键字过滤、表格展示、CSV 导出（含 BOM 与转义）。
- **存储**：`oplog!` 宏落点由 tracing 改为独立 JSONL（`logs/audit-YYYY-MM-DD.jsonl`，保留 7 天）。
- **操作结果（二期）**：每条记录带结果态（进行中 / 成功 / 失败）与失败原因，页面新增结果列与失败红标过滤；`install` / `upgrade` / `start` 等后台任务记录进行中与完成结果。覆盖 26 处同步命令 + 服务组拉起的成员启动。
- **分页**：每页 20/50/100/200 可选 + 总数与上下页。
- 启停 / 重启记录补充 detail（版本 + 实例 id）。

### 系统监控与告警
- **指标历史持久化**：新增 30s 常驻采样器（整机 + 运行中软件 / SpringBoot），落盘保留 7 天（此前该文件有读无写、曲线只在内存保留 2 分钟）；保留天数可在设置页配置（1–90 天，默认 7）。
- **趋势曲线改读后端持久化序列**，移除前端历史累积与硬编码阈值。
- **告警规则**：4 个阈值（默认 90，可配）入设置；触发 / 恢复记审计 + 全局 toast 提示；顺带修掉「告警只在监控页打开时才生效」。
- **告警通知渠道**：webhook（支持 json / 钉钉加签 / 企业微信 / 飞书四种格式）+ SMTP 邮件；设置页提供「测试」按钮走真实发送。
- **监控中心页**（`/monitor`）：KPI×3 + 进程资源列表 + 搜索；与首页共用 `useRunningSoftware()` composable。
- **首页瘦身**：只保留整机监控，「运行中服务与应用」改为末尾折叠块。
- **进程资源监控**：静态缓存 `System` 跨调用计算 CPU%；修正 mysqld 等 fork 型服务统计（累加后代进程）；落盘采样用独立实例取 30s 平均 CPU。

### 崩溃自愈（看门狗）
- **软件 / SpringBoot / Node 三实体**进程意外退出后延迟 2s 自动拉起；连续失败 3 次放弃（Error + toast + 审计）。
- 失败计数规则：观察到意外退出即 +1，健康即清零。
- 主动停止不再被误拉起（Node 停止先置 Stopping）；未开自动重启的进程被外部结束时统一「状态转 Error + 提示」，且状态监听提升到 `App.vue` 全局注册，切页不再丢失事件。
- 软件与 Node 的启动设置支持开启自动重启。

### 服务组与编排
- **统一启动编排**：依赖就绪后再启动依赖方（`wait_dependency_ready`）。
- **软件级依赖拓扑**可视化；**进程端口图谱**（netstat 采集运行中软件的监听端口 + 冲突诊断，卡片嵌入）。
- 服务组拉起的成员启动补记操作结果。

### 软件管理
- **Nacos 集群模式**：单节点加入外部集群（填 `cluster_nodes` 多行 `ip:port`，OPX 写 `conf/cluster.conf`，cluster 时强校验 `storage=mysql`）；配置表单新增 `Textarea` 字段类型。
- Nacos 控制台链接按版本区分（3.x 不带 `/nacos`）；数据库字段按 storage 实时显示。

### 设置与界面
- **设置页 Tab 分区**：通用 / 监控与告警 / 域名与 DNS 三个 Tab，选中态记入 URL（`?tab=`，刷新与直达均保持，非法值回退「通用」）。
- 配置面板官方文档外链。
- 站点对话框改为头部固定、仅表单区滚动。

---

## 问题修复

- **全仓出站请求统一关闭环境变量代理**：此前 DDNS / ACME 的 DNS API 调用会被宿主的 `ALL_PROXY` 劫持导致连接失败。
- **DNSPod 端点修正**：`DescribeXxx`/`CreateXxx` 属 `dnspod.tencentcloudapi.com`（`dnsapi` 是老端点命名）。
- **「无记录」错误码修正**：实际返回 `ResourceNotFound.NoDataOfRecord`（原写 `NoFoundData`）。
- **公网 IP 源修正**：改家族专用端点，避免双栈网络下把 IPv6 当 IPv4 源导致永久失败。
- **华为云记录集 API 改用 v2.1 路径**（v2 会 404）；非 JSON 响应显式报错，避免退化成 Null 误诊。
- **Cloudflare zone 查询分页**：避免账户 zone 超过 50 个时选错。
- **ACME**：验证完成后才清理 TXT 记录（避免过早删除导致验证失败）；staging / 生产账户凭据不跨目录复用；TXT 值剥离引号以满足可比较契约；错误链透出（Cloudflare 10000 给出区域作用域 `DNS:Edit` 权限提示）。
- **看门狗**：修正崩溃循环不达上限、主动停止被误拉起、状态卡死等问题。
- **指标**：修正历史读取失败覆写归零、进程键裁剪、告警态释放、原子写；历史解析失败留痕告警。
- **Spring Boot**：应用启停失败补 toast + catch（此前 try/finally 静默吞错）；启动编排等待依赖就绪。
- **审计**：CSV 导出时间改为标准格式；`oplog_fail!` 按 Display 取错误文本兼容 `anyhow::Error`；上一页被拦截修复。
- **i18n**：内存曲线标题硬编码中文在英文界面暴露。
- **界面**：长错误文本换行避免撑破对话框。

---

## 已知说明

- **DNS 服务商实测覆盖**：DNSPod（腾讯云）已完成 A / AAAA / TXT 全链路实机验证；**阿里云、华为云、Cloudflare 的 TXT 写入路径尚未实机验证**（开发环境无可用账号），单元测试已覆盖，待有账号时补验。验证脚本见 `src-tauri/tests/live_dns.rs`（`cargo test --test live_dns -- --ignored --nocapture`）。
- **华为云 zone 列表接口版本**（`/v2/zones` vs `/v2.1/zones`）未经实机确认，代码内有 `ponytail:` 注释标明失败模式与改法。
- 告警通知渠道中，**webhook 通用 JSON 格式已实机验证**（公共测试站），钉钉 / 企微 / 飞书专用格式为纯函数层单测覆盖。
- DNS 凭证沿用**明文存储**（`dns-accounts.json`），UI 已有风险提示；不做加密存储。
- 证书续期采用固定 15s 传播等待（非 TTL 感知）；`cert_expires_at` 按签发时间 +90 天估算。

---

# opx 0.4.0 发行说明

发布日期：2026-08-26

> 本版本聚焦「日志归档无缝查看」与「服务组运行增强」，新增 Node.js 运行时支持，并完成 Node 应用管理模块。

---

## 新增功能

### Node 应用管理
- 新增「Node 应用」模块：注册 JS/TS 服务（入口文件 + 启动参数 + 环境变量 + 自启顺序），一键启停、运行状态、日志尾部查看；使用已装 Node.js 运行时拉起，应用启动时按顺序自动拉起勾选应用。

### 日志
- **软件日志历史归档无缝续接**：日志查看器可读取按天滚动 / gzip 压缩的历史归档（如 logrotate 与 logback 常见形态），连续点「加载更早」自动跨档续读，无需切换操作。
- **应用日志按 level 多源**：Spring Boot 应用日志动态列出 debug / info / error / warn 等来源标签页，各源可查看 `logs/<日期目录>/<类型>.日期.序号.log.gz` 归档历史。
- **日志时间范围过滤**：查看器支持「开始 / 结束」时间筛选，按日志时间戳过滤已加载内容（配合「加载更早」检索历史时段）。
- **合并导出归档**：一键导出「当前日志源 + 全部历史归档」为单一文件（旧→新，.gz 自动解压）。
- **全局日志关键字搜索**：软件管理页新增「日志搜索」，跨所有已装软件的日志源与归档全文检索。
- **文件监听实时推送**：打开日志查看器即监听日志文件变化，新日志即时刷新（保留 1.5s 轮询兜底，断网亦可）。
- **应用日志查看器对齐**：应用日志补齐级别过滤 / 仅错误 / 正则 / 下载能力，与软件日志查看器一致。

### 服务组（Stack）
- **启动报告**：最近一次启动展示整组耗时与每个成员的启动耗时 / 结果（运行 / 失败及原因），持久化保存。
- **成员快捷入口**：运行面板成员卡新增端口一键打开浏览器、日志查看快捷入口（复用软件 / 应用两套日志查看器）。

### 软件仓库
- **Node.js 运行时**：新增 Runtime 类软件（内置 v20 / v22 LTS + 在线动态版本拉取），纯安装不启动，供 Node 应用管理消费。

### 界面
- **卡片图标与仓库同源**：软件管理图标改为 catalog 专属图标（与软件仓库一致），并语义化（PostgreSQL 象 / Redis 内存 / Node.js / Java……）。
- **分类底色补齐**：消息队列 / 搜索 / 时序分类配色。
- **服务组面板清理**：移除已实现的「自启 / 模板 / 启动报告」占位残留。

### 其他
- **Nacos 集群选项前置置灰**：配置表单中 cluster 模式禁用并提示「后续版本支持」，保留后续可扩展接入点（单机 standalone 行为不变）。

---

## 问题修复

- **应用日志实时轮询重复追加**：修复日志查看器把「重新读尾部」当作轮询增量、导致 `console.log` 无限重复刷新的问题；现在仅首次读取尾部，之后按字节偏移增量读取新增行。

---

# opx 0.3.0 发行说明

发布日期：2026-08-25

> opx（software-manager）是一个本地的软件安装、启动、监控与运维一体化管理工具。
> 本版本在 0.2.0 基础上完成第二轮规划（roadmap-2）全部内容，并修复多项启动问题。

---

## 新增功能

### 软件管理
- **软件仓库页**：按软件分类（数据库 / 运行时 / 缓存 / Web 服务器 / 对象存储 / 注册中心 / 消息队列 / 搜索 / 时序数据库）以 Tab 区分展示。
- **批量安装**：仓库页支持多选软件一次批量安装，实时显示下载 / 解压进度。
- **升级检测与一键升级**：已装列表自动对比在线版本，显示可升级目标并一键升级（替换式：停旧 → 备份 `.bak` → 装新 → 迁移数据 → 合并记录）；升级后可一键回滚恢复旧版本。
- **备份 / 恢复**：支持软件数据快照备份、历史快照恢复，以及「一键重置」回到初始状态；升级备份改为 zip 压缩包，减少磁盘占用。
- **卡片增强**：软件卡片显示运行端口，Web 可访问的软件（ES / InfluxDB / nginx / MinIO / Nacos / 自定义 Http 健康检查）提供可点击网页访问地址。
- **运行时软件（JRE / JDK）**：卡片仅保留「卸载」操作，由 Spring Boot 应用自动拉起，不参与启停 / 配置。

### 新增软件支持
- **数据库**：PostgreSQL、MongoDB
- **注册中心 / 配置中心**：Nacos
- **消息队列**：Kafka（KRaft 模式，无需 ZooKeeper，自动初始化存储）
- **搜索**：Elasticsearch（自动使用内置 JDK）
- **时序数据库**：InfluxDB
- **对象存储**：RustFS

### 服务组（Stack）
- 服务组编排与成员管理
- 一键启动 / 停止整组服务
- 监控页「服务组概览」卡片展示各组的运行 / 失败状态

### Spring Boot 应用管理
- 应用日志查看：支持**实时刷新开关、自动滚动开关、关键词搜索与高亮**
- 应用运行信息增强：GC 次数 / 耗时、运行包路径、依赖候选检测、自动重启、换包
- 环境变量、JVM 参数、启动依赖配置

### 网站管理
- 监听端口与路由规则（静态目录 / 反向代理）
- 自定义 Nginx 配置（表单生成 / 源码编辑双模式）
- 反向代理增强与 SSL 预留

### 系统监控
- 概览卡片（CPU / 内存 / 磁盘 / 网络）+ 趋势图
- **进程资源监控**：逐进程 CPU / 内存实时采样，行内展开趋势图，CPU / 内存超阈值告警
- 服务组概览卡片重排至 CPU / 内存趋势之下，进程监控紧随服务组之后

### 运维增强
- 统一操作日志（按日滚动审计，保留 7 天）与日志自动清理
- 配置导出 / 导入备份

---

## 问题修复

- **Elasticsearch 启动失败**：改用内置 JDK，规避安装的外部 JRE 缺失 `jdk.attach` 模块导致 `org.elasticsearch.entitlement` 加载失败；抑制 AWS S3 区域告警噪声。
- **Kafka 启动失败（系统找不到指定的路径）**：tgz 解压产生嵌套目录的路径解析、`CLASSPATH` 过长改为通配符引用、`log.dirs` 路径中的反斜杠转义问题（`\t` 等被 Java properties 误解析）。
- **软件日志阅读**：修复软件日志尾部读取的偏移分页错位。

---

## 已知说明

- 网站 SSL 证书功能为预留，近期版本开放。
- 升级仅在使用说明所述的镜像 / 后端可用时生效，离线环境下以内置默认版本为准。
