# opx 0.4.0 发行说明

发布日期：2026-08-26

> 本版本聚焦「日志归档无缝查看」与「服务组运行增强」，并新增 Node.js 运行时支持。

---

## 新增功能

### 日志
- **软件日志历史归档无缝续接**：日志查看器可读取按天滚动 / gzip 压缩的历史归档（如 logrotate 与 logback 常见形态），连续点「加载更早」自动跨档续读，无需切换操作。
- **应用日志按 level 多源**：Spring Boot 应用日志动态列出 debug / info / error / warn 等来源标签页，各源可查看 `logs/<日期目录>/<类型>.日期.序号.log.gz` 归档历史。

### 服务组（Stack）
- **启动报告**：最近一次启动展示整组耗时与每个成员的启动耗时 / 结果（运行 / 失败及原因），持久化保存。
- **成员快捷入口**：运行面板成员卡新增端口一键打开浏览器、日志查看快捷入口（复用软件 / 应用两套日志查看器）。

### 软件仓库
- **Node.js 运行时**：新增 Runtime 类软件（内置 v20 / v22 LTS + 在线动态版本拉取），纯安装不启动，供未来 Node 应用管理消费。

### 其他
- **Nacos 集群选项前置置灰**：配置表单中 cluster 模式禁用并提示「后续版本支持」，保留后续可扩展接入点（单机 standalone 行为不变）。

---

# opx 0.3.0 发行说明

发布日期：2026-08-25

> opx（software-manager）是一个本地的软件安装、启动、监控与运维一体化管理工具。
> 本版本在 0.2.0 基础上完成第二轮规划（roadmap-2）全部内容，并修复多项启动问题。

---

## 新增功能

### 软件管理
- **软件仓库页**：按软件分类（数据库 / 运行时 / 缓存 / Web 服务器 / 对象存储 / 注册中心 / 消息队列 / 搜索 / 时序数据库）以 Tab 区分展示。
- **批量安装**：仓库页支持多选软件一次批量安装，实时显示下载 / 解压进度。
- **升级检测与一键升级**：已装列表自动对比在线版本，显示可升级目标并一键升级（替换式：停旧 → 备份 `.bak` → 装新 → 迁移数据 → 合并记录）；升级后可一键回滚恢复旧版本。
- **备份 / 恢复**：支持软件数据快照备份、历史快照恢复，以及「一键重置」回到初始状态；升级备份改为 zip 压缩包，减少磁盘占用。
- **卡片增强**：软件卡片显示运行端口，Web 可访问的软件（ES / InfluxDB / nginx / MinIO / Nacos / 自定义 Http 健康检查）提供可点击网页访问地址。
- **运行时软件（JRE / JDK）**：卡片仅保留「卸载」操作，由 Spring Boot 应用自动拉起，不参与启停 / 配置。

### 新增软件支持
- **数据库**：PostgreSQL、MongoDB
- **注册中心 / 配置中心**：Nacos
- **消息队列**：Kafka（KRaft 模式，无需 ZooKeeper，自动初始化存储）
- **搜索**：Elasticsearch（自动使用内置 JDK）
- **时序数据库**：InfluxDB
- **对象存储**：RustFS

### 服务组（Stack）
- 服务组编排与成员管理
- 一键启动 / 停止整组服务
- 监控页「服务组概览」卡片展示各组的运行 / 失败状态

### Spring Boot 应用管理
- 应用日志查看：支持**实时刷新开关、自动滚动开关、关键词搜索与高亮**
- 应用运行信息增强：GC 次数 / 耗时、运行包路径、依赖候选检测、自动重启、换包
- 环境变量、JVM 参数、启动依赖配置

### 网站管理
- 监听端口与路由规则（静态目录 / 反向代理）
- 自定义 Nginx 配置（表单生成 / 源码编辑双模式）
- 反向代理增强与 SSL 预留

### 系统监控
- 概览卡片（CPU / 内存 / 磁盘 / 网络）+ 趋势图
- **进程资源监控**：逐进程 CPU / 内存实时采样，行内展开趋势图，CPU / 内存超阈值告警
- 服务组概览卡片重排至 CPU / 内存趋势之下，进程监控紧随服务组之后

### 运维增强
- 统一操作日志（按日滚动审计，保留 7 天）与日志自动清理
- 配置导出 / 导入备份

---

## 问题修复

- **Elasticsearch 启动失败**：改用内置 JDK，规避安装的外部 JRE 缺失 `jdk.attach` 模块导致 `org.elasticsearch.entitlement` 加载失败；抑制 AWS S3 区域告警噪声。
- **Kafka 启动失败（系统找不到指定的路径）**：tgz 解压产生嵌套目录的路径解析、`CLASSPATH` 过长改为通配符引用、`log.dirs` 路径中的反斜杠转义问题（`\t` 等被 Java properties 误解析）。
- **软件日志阅读**：修复软件日志尾部读取的偏移分页错位。

---

## 已知说明

- 网站 SSL 证书功能为预留，近期版本开放。
- 升级仅在使用说明所述的镜像 / 后端可用时生效，离线环境下以内置默认版本为准。
