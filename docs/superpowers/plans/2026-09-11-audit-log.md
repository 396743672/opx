# 操作记录（审计日志）一期 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把已有 `oplog!` 记录的操作落到独立 JSONL 审计文件，并提供侧边栏「操作记录」页（过滤、搜索、统计、导出）。

**Architecture:** 新增 `services/software_manager/audit.rs` 负责追加/查询/统计/导出；`oplog!` 宏体改为调用 `audit::record`（28 个调用点零改动）；新增 `commands/audit.rs` 暴露三个命令；前端新增 `AuditLogPage.vue` + 路由 + 侧边栏入口。一期不记录操作成功/失败。

**Tech Stack:** Rust（tauri v2, serde, chrono, serde_json — 均已在用）、Vue 3 + TypeScript + vue-router + vue-i18n + `@tauri-apps/plugin-dialog`。

## Global Constraints

- 分支：`feat/audit-log`（已创建，spec 已提交 `49415cc`）。
- 不引入新依赖（serde / serde_json / chrono / once_cell 已存在）。
- 审计文件：`<logs_dir>/audit-YYYY-MM-DD.jsonl`，每行一个 JSON 对象；`logs_dir` 取 `crate::utils::paths::logs_dir()`。
- 保留策略：7 天，沿用 `audit_log::DEFAULT_RETAIN_DAYS`。
- i18n 键必须**同时**加到 `src/locales/zh-CN.ts` 与 `src/locales/en-US.ts`。
- 验证命令：后端 `cargo test --lib`（在 `src-tauri/` 下）；前端 `npx vue-tsc --noEmit`（在仓库根）。
- 一期**不**引入 `result`/`error` 字段（二期）。
- 提交信息用中文，遵循 `feat(...)` / `test(...)` / `chore(...)` 风格；不要加 Co-Authored-By。

---

### Task 1: 审计存储核心（record + 数据结构）

**Files:**
- Create: `src-tauri/src/services/software_manager/audit.rs`
- Modify: `src-tauri/src/services/software_manager/mod.rs`（新增 `pub mod audit;`）
- Test: `src-tauri/src/services/software_manager/audit.rs`（内联 `#[cfg(test)] mod tests`）

**Interfaces:**
- Consumes: `crate::utils::paths::logs_dir() -> PathBuf`
- Produces:
  - `pub struct AuditEntry { pub ts: String, pub action: String, pub target: String, pub detail: String }`
  - `pub fn record(action: &str, target: &str, detail: &str)`

- [ ] **Step 1: 写失败的测试**

在新建的 `audit.rs` 末尾写入：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    /// 唯一标记，避免测试与真实记录/其他测试互相干扰。
    fn uniq() -> String {
        format!("__qa_{}", chrono::Local::now().timestamp_nanos_opt().unwrap_or(0))
    }

    #[test]
    fn record_appends_parseable_entry() {
        let u = uniq();
        record("__qa_record", &format!("{u}_target"), &format!("{u}_detail"));
        let path = file_for(chrono::Local::now().date_naive());
        let content = std::fs::read_to_string(&path).expect("audit file exists");
        let hit = content
            .lines()
            .filter_map(|l| serde_json::from_str::<AuditEntry>(l).ok())
            .find(|e| e.action == "__qa_record" && e.target == format!("{u}_target"));
        let hit = hit.expect("entry appended");
        assert_eq!(hit.detail, format!("{u}_detail"));
        assert!(!hit.ts.is_empty());
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd src-tauri && cargo test --lib audit::tests::record_appends_parseable_entry`
Expected: 编译失败（`file_for` / `record` / `AuditEntry` 未定义）

- [ ] **Step 3: 写最小实现**

在 `audit.rs` 顶部写入（测试模块之前）：

```rust
//! 操作审计存储：每条操作一行 JSON 追加到 logs/audit-YYYY-MM-DD.jsonl。
//! 与普通运行日志分离，供「操作记录」页查询/统计/导出。

use std::io::Write;
use std::path::PathBuf;

use serde::{Deserialize, Serialize};

use crate::utils::paths;

/// 单条操作记录。一期不含结果字段；二期新增 result/error 时须带 #[serde(default)]。
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct AuditEntry {
    pub ts: String,
    pub action: String,
    pub target: String,
    #[serde(default)]
    pub detail: String,
}

/// 审计文件路径：<logs_dir>/audit-YYYY-MM-DD.jsonl
fn file_for(date: chrono::NaiveDate) -> PathBuf {
    paths::logs_dir().join(format!("audit-{}.jsonl", date.format("%Y-%m-%d")))
}

/// 追加一条操作记录（同步写盘：保证 quit/exit 等退出前调用不丢尾部）。
pub fn record(action: &str, target: &str, detail: &str) {
    let entry = AuditEntry {
        ts: chrono::Local::now().to_rfc3339(),
        action: action.to_string(),
        target: target.to_string(),
        detail: detail.to_string(),
    };
    let path = file_for(chrono::Local::now().date_naive());
    if let Some(parent) = path.parent() {
        let _ = std::fs::create_dir_all(parent);
    }
    if let Ok(line) = serde_json::to_string(&entry) {
        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(&path) {
            let _ = writeln!(f, "{}", line);
        }
    }
}
```

在 `src-tauri/src/services/software_manager/mod.rs` 的模块列表中新增一行：

```rust
pub mod audit;
```

（放在 `pub mod audit_log;` 之前或之后均可。）

- [ ] **Step 4: 运行测试确认通过**

Run: `cd src-tauri && cargo test --lib audit::tests::record_appends_parseable_entry`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/services/software_manager/audit.rs src-tauri/src/services/software_manager/mod.rs
git commit -m "feat(audit): 审计存储核心（JSONL 追加）"
```

---

### Task 2: 查询（按天/action/关键字过滤 + 截断标记）

**Files:**
- Modify: `src-tauri/src/services/software_manager/audit.rs`
- Test: `src-tauri/src/services/software_manager/audit.rs`

**Interfaces:**
- Consumes: `AuditEntry`, `file_for`, `record`
- Produces:
  - `pub struct AuditQuery { pub entries: Vec<AuditEntry>, pub truncated: bool }`
  - `pub fn query(days: u64, action: Option<&str>, keyword: Option<&str>, limit: usize) -> AuditQuery`
  - `fn read_days(days: u64) -> Vec<AuditEntry>`

- [ ] **Step 1: 写失败的测试**

在 `tests` 模块内追加：

```rust
    #[test]
    fn query_filters_by_action_and_keyword() {
        let u = uniq();
        record("__qa_q_action", &format!("{u}_target"), "");
        record("__qa_q_other", &format!("{u}_target"), "");
        let q = query(1, Some("__qa_q_action"), Some(&u), 100);
        assert_eq!(q.entries.len(), 1);
        assert_eq!(q.entries[0].action, "__qa_q_action");
        assert!(!q.truncated);
    }

    #[test]
    fn query_is_case_insensitive_on_keyword() {
        let u = uniq();
        record("__qa_q_case", &format!("{u}_MixedCase"), "");
        assert_eq!(query(1, Some("__qa_q_case"), Some(&u.to_lowercase()), 100).entries.len(), 1);
        assert_eq!(query(1, Some("__qa_q_case"), Some("mixedcase"), 100).entries.len(), 1);
    }

    #[test]
    fn query_marks_truncated_when_over_limit() {
        let u = uniq();
        for i in 0..3 {
            record("__qa_q_trunc", &format!("{u}_{i}"), "");
        }
        let q = query(1, Some("__qa_q_trunc"), Some(&u), 2);
        assert_eq!(q.entries.len(), 2);
        assert!(q.truncated);
    }
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd src-tauri && cargo test --lib audit::tests::query_`
Expected: 编译失败（`query` / `AuditQuery` 未定义）

- [ ] **Step 3: 写最小实现**

在 `audit.rs`（`record` 之后、`tests` 之前）追加：

```rust
/// 查询结果（entries 按 ts 倒序）
#[derive(Debug, Clone, Serialize)]
pub struct AuditQuery {
    pub entries: Vec<AuditEntry>,
    pub truncated: bool,
}

/// 读取最近 days 天的全部条目（未过滤、未排序）。无法解析的行跳过，不 panic。
fn read_days(days: u64) -> Vec<AuditEntry> {
    let today = chrono::Local::now().date_naive();
    let mut out = Vec::new();
    for i in 0..days.max(1) as i64 {
        let path = file_for(today - chrono::Duration::days(i));
        let Ok(content) = std::fs::read_to_string(&path) else { continue };
        for line in content.lines() {
            let line = line.trim();
            if line.is_empty() {
                continue;
            }
            if let Ok(e) = serde_json::from_str::<AuditEntry>(line) {
                out.push(e);
            }
        }
    }
    out
}

fn matches_filters(e: &AuditEntry, action: Option<&str>, keyword: Option<&str>) -> bool {
    if let Some(a) = action {
        if !a.is_empty() && e.action != a {
            return false;
        }
    }
    if let Some(k) = keyword {
        let k = k.trim().to_lowercase();
        if !k.is_empty() {
            let haystack = format!("{} {} {}", e.action, e.target, e.detail).to_lowercase();
            if !haystack.contains(&k) {
                return false;
            }
        }
    }
    true
}

/// 查询：天范围 + action 精确 + keyword 子串（大小写不敏感），ts 倒序，最多 limit 条。
pub fn query(days: u64, action: Option<&str>, keyword: Option<&str>, limit: usize) -> AuditQuery {
    let mut entries: Vec<AuditEntry> = read_days(days)
        .into_iter()
        .filter(|e| matches_filters(e, action, keyword))
        .collect();
    // ts 为本地时区 RFC3339，同偏移下字符串序即时间序
    entries.sort_by(|a, b| b.ts.cmp(&a.ts));
    let truncated = entries.len() > limit;
    entries.truncate(limit);
    AuditQuery { entries, truncated }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd src-tauri && cargo test --lib audit::tests`
Expected: PASS（Task 1 与 Task 2 的测试全过）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/services/software_manager/audit.rs
git commit -m "feat(audit): 审计查询（天/action/关键字过滤 + 截断标记）"
```

---

### Task 3: 统计聚合

**Files:**
- Modify: `src-tauri/src/services/software_manager/audit.rs`
- Test: `src-tauri/src/services/software_manager/audit.rs`

**Interfaces:**
- Consumes: `read_days`
- Produces:
  - `pub struct ActionCount { pub action: String, pub count: u64 }`
  - `pub struct AuditStats { pub today: u64, pub total: u64, pub by_action: Vec<ActionCount>, pub last_ts: Option<String> }`
  - `pub fn stats(days: u64) -> AuditStats`

- [ ] **Step 1: 写失败的测试**

```rust
    #[test]
    fn stats_counts_today_total_and_by_action() {
        let u = uniq();
        record("__qa_stats", &u, "");
        record("__qa_stats", &u, "");
        let s = stats(1);
        assert!(s.total >= 2);
        assert!(s.today >= 2);
        let c = s
            .by_action
            .iter()
            .find(|c| c.action == "__qa_stats")
            .expect("action counted");
        assert!(c.count >= 2);
        assert!(s.last_ts.is_some());
    }
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd src-tauri && cargo test --lib audit::tests::stats_counts_today_total_and_by_action`
Expected: 编译失败（`stats` / `AuditStats` 未定义）

- [ ] **Step 3: 写最小实现**

```rust
#[derive(Debug, Clone, Serialize, PartialEq)]
pub struct ActionCount {
    pub action: String,
    pub count: u64,
}

#[derive(Debug, Clone, Serialize)]
pub struct AuditStats {
    pub today: u64,
    pub total: u64,
    pub by_action: Vec<ActionCount>,
    pub last_ts: Option<String>,
}

/// 统计最近 days 天：今日条数、总数、按 action 计数（降序，同数按 action 升序）、最近时间。
pub fn stats(days: u64) -> AuditStats {
    let all = read_days(days);
    let today_prefix = chrono::Local::now().date_naive().format("%Y-%m-%d").to_string();
    let today = all.iter().filter(|e| e.ts.starts_with(&today_prefix)).count() as u64;

    let mut counts: std::collections::HashMap<String, u64> = std::collections::HashMap::new();
    for e in &all {
        *counts.entry(e.action.clone()).or_insert(0) += 1;
    }
    let mut by_action: Vec<ActionCount> = counts
        .into_iter()
        .map(|(action, count)| ActionCount { action, count })
        .collect();
    by_action.sort_by(|a, b| b.count.cmp(&a.count).then(a.action.cmp(&b.action)));

    let last_ts = all.iter().map(|e| e.ts.clone()).max();
    AuditStats {
        today,
        total: all.len() as u64,
        by_action,
        last_ts,
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd src-tauri && cargo test --lib audit::tests`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/services/software_manager/audit.rs
git commit -m "feat(audit): 统计聚合（今日/总数/按操作计数/最近时间）"
```

---

### Task 4: CSV 导出

**Files:**
- Modify: `src-tauri/src/services/software_manager/audit.rs`
- Test: `src-tauri/src/services/software_manager/audit.rs`

**Interfaces:**
- Consumes: `query`
- Produces:
  - `fn csv_field(s: &str) -> String`
  - `pub fn export_csv(days: u64, action: Option<&str>, keyword: Option<&str>, dest_path: &str) -> Result<(), String>`

- [ ] **Step 1: 写失败的测试**

```rust
    #[test]
    fn csv_field_escapes_special_chars() {
        assert_eq!(csv_field("plain"), "plain");
        assert_eq!(csv_field("a,b"), "\"a,b\"");
        assert_eq!(csv_field("he said \"hi\""), "\"he said \"\"hi\"\"\"");
        assert_eq!(csv_field("l1\nl2"), "\"l1\nl2\"");
    }

    #[test]
    fn export_csv_writes_header_and_rows() {
        let u = uniq();
        record("__qa_csv", &format!("{u},comma"), "");
        let dest = paths::logs_dir().join(format!("{u}.csv"));
        export_csv(1, Some("__qa_csv"), Some(&u), dest.to_str().unwrap()).unwrap();
        let content = std::fs::read_to_string(&dest).unwrap();
        assert!(content.starts_with('\u{feff}'), "BOM for Excel");
        assert!(content.contains("时间,操作,目标,详情"));
        assert!(content.contains("__qa_csv"));
        let _ = std::fs::remove_file(&dest);
    }
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd src-tauri && cargo test --lib audit::tests::csv_`
Expected: 编译失败（`csv_field` / `export_csv` 未定义）

- [ ] **Step 3: 写最小实现**

```rust
/// CSV 字段转义：含逗号/引号/换行时用双引号包裹，内部引号翻倍。
fn csv_field(s: &str) -> String {
    if s.contains(',') || s.contains('"') || s.contains('\n') || s.contains('\r') {
        format!("\"{}\"", s.replace('"', "\"\""))
    } else {
        s.to_string()
    }
}

/// 按过滤条件导出 CSV（UTF-8 BOM，便于 Excel 正确识别中文）到 dest_path。
pub fn export_csv(
    days: u64,
    action: Option<&str>,
    keyword: Option<&str>,
    dest_path: &str,
) -> Result<(), String> {
    let q = query(days, action, keyword, usize::MAX);
    let mut out = String::from("\u{feff}时间,操作,目标,详情\n");
    for e in q.entries {
        out.push_str(&format!(
            "{},{},{},{}\n",
            csv_field(&e.ts),
            csv_field(&e.action),
            csv_field(&e.target),
            csv_field(&e.detail)
        ));
    }
    std::fs::write(dest_path, out).map_err(|e| e.to_string())
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd src-tauri && cargo test --lib audit::tests`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/services/software_manager/audit.rs
git commit -m "feat(audit): CSV 导出（含转义与 BOM）"
```

---

### Task 5: `oplog!` 宏切换落点 + 保留清理扩展

**Files:**
- Modify: `src-tauri/src/services/software_manager/audit_log.rs`
- Test: `src-tauri/src/services/software_manager/audit_log.rs`（新增 `#[cfg(test)] mod tests`）

**Interfaces:**
- Consumes: `audit::record`, `DEFAULT_RETAIN_DAYS`
- Produces: `cleanup_old_logs(log_dir: &Path, retain_days: u64)`（行为扩展：同时删超期 `audit-*.jsonl`）

- [ ] **Step 1: 写失败的测试**

在 `audit_log.rs` 末尾追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn uniq_dir() -> std::path::PathBuf {
        let d = std::env::temp_dir().join(format!(
            "__qa_auditlog_{}",
            chrono::Local::now().timestamp_nanos_opt().unwrap_or(0)
        ));
        std::fs::create_dir_all(&d).unwrap();
        d
    }

    #[test]
    fn cleanup_removes_old_audit_files_keeps_recent() {
        let dir = uniq_dir();
        let old = dir.join("audit-2000-01-01.jsonl");
        let recent = dir.join(format!(
            "audit-{}.jsonl",
            chrono::Local::now().date_naive().format("%Y-%m-%d")
        ));
        std::fs::write(&old, "{}").unwrap();
        std::fs::write(&recent, "{}").unwrap();

        cleanup_old_logs(&dir, DEFAULT_RETAIN_DAYS);

        assert!(!old.exists(), "old audit file removed");
        assert!(recent.exists(), "recent audit file kept");
        let _ = std::fs::remove_dir_all(&dir);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd src-tauri && cargo test --lib audit_log::tests::cleanup_removes_old_audit_files_keeps_recent`
Expected: FAIL（`old.exists()` 为 true，审计文件未被清理）

- [ ] **Step 3: 实现：宏切落点 + 清理扩展**

把 `audit_log.rs` 的 `oplog!` 宏定义替换为：

```rust
/// 操作日志宏 — 写入独立审计 JSONL（供「操作记录」页查询）
#[macro_export]
macro_rules! oplog {
    ($action:expr, $target:expr) => {
        $crate::services::software_manager::audit::record($action, $target, "");
    };
    ($action:expr, $target:expr, $detail:expr) => {
        $crate::services::software_manager::audit::record($action, $target, $detail);
    };
}
```

在 `cleanup_old_logs` 内、遍历目录的循环中，对 `name` 增加审计文件分支：

```rust
                // 审计文件 audit-YYYY-MM-DD.jsonl：同规则清理
                if let Some(date_str) = name
                    .strip_prefix("audit-")
                    .and_then(|s| s.strip_suffix(".jsonl"))
                {
                    if let Ok(date) = chrono::NaiveDate::parse_from_str(date_str, "%Y-%m-%d") {
                        if date < cutoff_date {
                            let _ = std::fs::remove_file(entry.path());
                        }
                    }
                }
```

（与既有的 `software-manager.log.` 分支并列；注意不要改动既有分支。）

- [ ] **Step 4: 运行测试确认通过 + 回归**

Run: `cd src-tauri && cargo test --lib`
Expected: 全绿（含既有测试；确认 `oplog!` 改动未破坏编译）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/services/software_manager/audit_log.rs
git commit -m "feat(audit): oplog 宏落点改为审计 JSONL，保留清理覆盖审计文件"
```

---

### Task 6: 后端命令 + 注册

**Files:**
- Create: `src-tauri/src/commands/audit.rs`
- Modify: `src-tauri/src/commands/mod.rs`（新增 `pub mod audit;`）
- Modify: `src-tauri/src/lib.rs`（`invoke_handler` 注册三个命令）

**Interfaces:**
- Consumes: `audit::{query, stats, export_csv, AuditQuery, AuditStats}`
- Produces（前端 invoke 名 → 参数）:
  - `list_audit_entries(days: number, action?: string, keyword?: string, limit?: number) -> AuditQuery`
  - `audit_stats(days: number) -> AuditStats`
  - `export_audit_entries(days: number, action?: string, keyword?: string, destPath: string) -> void`

- [ ] **Step 1: 新建命令文件**

`src-tauri/src/commands/audit.rs`：

```rust
use crate::services::software_manager::audit;

/// 查询操作记录（默认上限 2000 条，超出以 truncated 标记）
#[tauri::command]
pub fn list_audit_entries(
    days: u64,
    action: Option<String>,
    keyword: Option<String>,
    limit: Option<usize>,
) -> audit::AuditQuery {
    audit::query(days, action.as_deref(), keyword.as_deref(), limit.unwrap_or(2000))
}

/// 操作记录概览统计
#[tauri::command]
pub fn audit_stats(days: u64) -> audit::AuditStats {
    audit::stats(days)
}

/// 按过滤条件导出 CSV 到 dest_path
#[tauri::command]
pub fn export_audit_entries(
    days: u64,
    action: Option<String>,
    keyword: Option<String>,
    dest_path: String,
) -> Result<(), String> {
    audit::export_csv(days, action.as_deref(), keyword.as_deref(), &dest_path)
}
```

- [ ] **Step 2: 注册模块与命令**

`src-tauri/src/commands/mod.rs` 新增：

```rust
pub mod audit;
```

`src-tauri/src/lib.rs` 的 `tauri::generate_handler![...]` 列表中（建议放在 `commands::software::...` 之前任一行附近）新增三行：

```rust
            commands::audit::list_audit_entries,
            commands::audit::audit_stats,
            commands::audit::export_audit_entries,
```

- [ ] **Step 3: 编译验证**

Run: `cd src-tauri && cargo check --lib`
Expected: Finished，无错误

- [ ] **Step 4: 提交**

```bash
git add src-tauri/src/commands/audit.rs src-tauri/src/commands/mod.rs src-tauri/src/lib.rs
git commit -m "feat(audit): 暴露查询/统计/导出三个命令"
```

---

### Task 7: 前端类型 + 审计页组件 + i18n

**Files:**
- Create: `src/models/audit.ts`
- Create: `src/modules/audit/pages/AuditLogPage.vue`
- Modify: `src/locales/zh-CN.ts`、`src/locales/en-US.ts`

**Interfaces:**
- Consumes: 命令 `list_audit_entries` / `audit_stats` / `export_audit_entries`
- Produces: 组件 `AuditLogPage.vue`（默认导出）

- [ ] **Step 1: 新增前端类型 `src/models/audit.ts`**

```ts
/// 操作记录（对应后端 services/software_manager/audit.rs）

export interface AuditEntry {
  ts: string
  action: string
  target: string
  detail: string
}

export interface AuditQuery {
  entries: AuditEntry[]
  truncated: boolean
}

export interface ActionCount {
  action: string
  count: number
}

export interface AuditStats {
  today: number
  total: number
  by_action: ActionCount[]
  last_ts: string | null
}
```

- [ ] **Step 2: 新增 i18n 键**

在 `src/locales/zh-CN.ts` 顶层对象内新增：

```ts
  auditLog: '操作记录',
  auditToday: '今日操作',
  auditTotal: '近 7 天操作',
  auditTopActions: '高频操作',
  auditLast: '最近操作',
  auditActionCol: '操作',
  auditTargetCol: '目标',
  auditDetailCol: '详情',
  auditTimeCol: '时间',
  auditKeyword: '关键字',
  auditKeywordPlaceholder: '搜索目标 / 详情',
  auditAllActions: '全部操作',
  auditRangeToday: '今天',
  auditRange3d: '近 3 天',
  auditRange7d: '近 7 天',
  auditExport: '导出 CSV',
  auditEmpty: '暂无操作记录',
  auditTruncated: '仅显示最近 {n} 条，可用关键字缩小范围',
  auditExportDone: '已导出',
  auditNone: '无',
```

在 `src/locales/en-US.ts` 顶层对象内新增：

```ts
  auditLog: 'Audit Log',
  auditToday: 'Today',
  auditTotal: 'Last 7 days',
  auditTopActions: 'Top actions',
  auditLast: 'Last activity',
  auditActionCol: 'Action',
  auditTargetCol: 'Target',
  auditDetailCol: 'Detail',
  auditTimeCol: 'Time',
  auditKeyword: 'Keyword',
  auditKeywordPlaceholder: 'Search target / detail',
  auditAllActions: 'All actions',
  auditRangeToday: 'Today',
  auditRange3d: 'Last 3 days',
  auditRange7d: 'Last 7 days',
  auditExport: 'Export CSV',
  auditEmpty: 'No audit records',
  auditTruncated: 'Showing latest {n} records; narrow with a keyword',
  auditExportDone: 'Exported',
  auditNone: 'N/A',
```

- [ ] **Step 3: 新建页面 `src/modules/audit/pages/AuditLogPage.vue`**

```vue
<template>
  <div class="animate-fade-in">
    <PageHeader icon="mdi:history" :title="$t('auditLog')">
      <template #actions>
        <button class="btn" @click="loadAll" :disabled="loading">
          <Icon icon="mdi:refresh" /> {{ $t('refresh') }}
        </button>
        <button class="btn" @click="onExport" :disabled="exporting || entries.length === 0">
          <Icon icon="mdi:download" /> {{ $t('auditExport') }}
        </button>
      </template>
    </PageHeader>

    <!-- 概览 -->
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-4">
      <StatCard :label="$t('auditToday')" :value="String(stats?.today ?? 0)" icon="mdi:calendar-today" accent="primary" />
      <StatCard :label="$t('auditTotal')" :value="String(stats?.total ?? 0)" icon="mdi:counter" accent="chart-2" />
      <StatCard :label="$t('auditLast')" :value="lastTs" icon="mdi:clock-outline" accent="chart-3" />
      <div class="rounded-lg border border-border bg-card p-4 shadow-card">
        <div class="text-xs text-muted-foreground mb-2">{{ $t('auditTopActions') }}</div>
        <div v-if="!stats?.by_action.length" class="text-sm text-muted-foreground">—</div>
        <div v-else class="flex flex-col gap-1">
          <div v-for="a in stats.by_action.slice(0, 3)" :key="a.action" class="flex items-center justify-between text-sm">
            <span class="truncate">{{ a.action }}</span>
            <span class="tnum text-muted-foreground">{{ a.count }}</span>
          </div>
        </div>
      </div>
    </div>

    <!-- 过滤栏 -->
    <div class="flex flex-wrap items-center gap-2 mb-3">
      <select v-model.number="days" class="input" @change="reload">
        <option :value="1">{{ $t('auditRangeToday') }}</option>
        <option :value="3">{{ $t('auditRange3d') }}</option>
        <option :value="7">{{ $t('auditRange7d') }}</option>
      </select>
      <select v-model="action" class="input" @change="reload">
        <option value="">{{ $t('auditAllActions') }}</option>
        <option v-for="a in stats?.by_action ?? []" :key="a.action" :value="a.action">{{ a.action }}</option>
      </select>
      <input v-model="keyword" class="input grow" :placeholder="$t('auditKeywordPlaceholder')" @keyup.enter="reload" />
      <button class="btn" @click="reload"><Icon icon="mdi:magnify" /> {{ $t('auditKeyword') }}</button>
    </div>

    <div v-if="truncated" class="text-xs text-warning mb-2">
      <Icon icon="mdi:alert-outline" /> {{ $t('auditTruncated', { n: entries.length }) }}
    </div>

    <!-- 表格 -->
    <div v-if="loading" class="py-8 text-center text-sm text-muted-foreground">{{ $t('loading') }}</div>
    <div v-else-if="entries.length === 0" class="py-8 text-center text-sm text-muted-foreground">{{ $t('auditEmpty') }}</div>
    <table v-else class="audit-table">
      <thead>
        <tr>
          <th>{{ $t('auditTimeCol') }}</th>
          <th>{{ $t('auditActionCol') }}</th>
          <th>{{ $t('auditTargetCol') }}</th>
          <th>{{ $t('auditDetailCol') }}</th>
        </tr>
      </thead>
      <tbody>
        <tr v-for="(e, i) in entries" :key="e.ts + i">
          <td class="tnum">{{ formatTs(e.ts) }}</td>
          <td><span class="action-chip">{{ e.action }}</span></td>
          <td class="truncate-cell">{{ e.target }}</td>
          <td class="truncate-cell">{{ e.detail || '—' }}</td>
        </tr>
      </tbody>
    </table>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { invoke } from '@tauri-apps/api/core'
import { save } from '@tauri-apps/plugin-dialog'
import { Icon } from '@iconify/vue'
import { useI18n } from 'vue-i18n'
import PageHeader from '@/components/PageHeader.vue'
import StatCard from '@/components/StatCard.vue'
import { toast } from '@/composables/useToast'
import type { AuditEntry, AuditStats } from '@/models/audit'

const { t } = useI18n()

const days = ref(7)
const action = ref('')
const keyword = ref('')
const entries = ref<AuditEntry[]>([])
const stats = ref<AuditStats | null>(null)
const truncated = ref(false)
const loading = ref(false)
const exporting = ref(false)

const lastTs = computed(() => (stats.value?.last_ts ? formatTs(stats.value.last_ts) : t('auditNone')))

function formatTs(iso: string): string {
  const d = new Date(iso)
  return Number.isNaN(d.getTime()) ? iso : d.toLocaleString()
}

async function loadEntries() {
  loading.value = true
  try {
    const q = await invoke<{ entries: AuditEntry[]; truncated: boolean }>('list_audit_entries', {
      days: days.value,
      action: action.value || null,
      keyword: keyword.value.trim() || null,
    })
    entries.value = q.entries
    truncated.value = q.truncated
  } catch (e) {
    toast(String(e), 'err')
  } finally {
    loading.value = false
  }
}

async function loadStats() {
  try {
    stats.value = await invoke<AuditStats>('audit_stats', { days: 7 })
  } catch {
    stats.value = null
  }
}

function reload() {
  loadEntries()
}

async function loadAll() {
  await Promise.all([loadEntries(), loadStats()])
}

async function onExport() {
  exporting.value = true
  try {
    const dest = await save({ defaultPath: 'audit-log.csv', title: t('auditExport') })
    if (!dest) return
    await invoke('export_audit_entries', {
      days: days.value,
      action: action.value || null,
      keyword: keyword.value.trim() || null,
      destPath: dest,
    })
    toast(t('auditExportDone'), 'ok')
  } catch (e) {
    toast(String(e), 'err')
  } finally {
    exporting.value = false
  }
}

onMounted(loadAll)
</script>

<style scoped>
.input {
  height: 34px;
  padding: 0 10px;
  background: var(--color-muted);
  border: 1px solid transparent;
  border-radius: 6px;
  color: var(--color-foreground);
  font-size: 13px;
  outline: none;
}
.input:focus {
  border-color: var(--color-primary);
  background: var(--color-card);
}
.grow {
  flex: 1;
  min-width: 160px;
}
.audit-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}
.audit-table th,
.audit-table td {
  text-align: left;
  padding: 8px 10px;
  border-bottom: 1px solid var(--color-border);
}
.audit-table th {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-muted-foreground);
}
.action-chip {
  display: inline-block;
  font-size: 11px;
  padding: 1px 6px;
  border-radius: 4px;
  background: color-mix(in oklch, var(--color-primary) 12%, transparent);
  color: var(--color-primary);
  font-weight: 600;
}
.truncate-cell {
  max-width: 320px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.tnum {
  font-variant-numeric: tabular-nums;
}
</style>
```

- [ ] **Step 4: 类型检查**

Run: `npx vue-tsc --noEmit`
Expected: 无输出（通过）。若报 `toast` 第二参类型不匹配，打开 `src/composables/useToast.ts` 确认签名并按实际类型调整（既有用法为 `toast(msg, 'ok' | 'err')`）。

- [ ] **Step 5: 提交**

```bash
git add src/models/audit.ts src/modules/audit/pages/AuditLogPage.vue src/locales/zh-CN.ts src/locales/en-US.ts
git commit -m "feat(audit): 操作记录页（概览/过滤/表格/导出）+ i18n"
```

---

### Task 8: 路由与侧边栏入口

**Files:**
- Modify: `src/router/index.ts`
- Modify: `src/layouts/Sidebar.vue`

**Interfaces:**
- Consumes: `AuditLogPage.vue`；i18n 键 `auditLog`
- Produces: 可访问路由 `/audit` 与侧边栏入口

- [ ] **Step 1: 注册路由**

在 `src/router/index.ts` 的 `routes` 数组中、`settings` 之前插入：

```ts
  {
    path: '/audit',
    name: 'audit',
    component: () => import('@/modules/audit/pages/AuditLogPage.vue'),
    meta: { title: 'auditLog' },
  },
```

- [ ] **Step 2: 侧边栏入口**

在 `src/layouts/Sidebar.vue` 的 `groups` 中，`management` 分组 `items` 末尾（`/stacks` 之后）追加：

```ts
      { path: '/audit', titleKey: 'auditLog', icon: 'mdi:history' },
```

- [ ] **Step 3: 类型检查 + 构建**

Run: `npx vue-tsc --noEmit`
Expected: 无输出（通过）

- [ ] **Step 4: 提交**

```bash
git add src/router/index.ts src/layouts/Sidebar.vue
git commit -m "feat(audit): 侧边栏与路由接入操作记录页"
```

---

### Task 9: 全量验证

**Files:** 无（仅运行验证）

- [ ] **Step 1: 后端全量测试**

Run: `cd src-tauri && cargo test --lib`
Expected: `test result: ok.` 且 0 failed（在既有 114 基础上新增约 8 条审计测试）

- [ ] **Step 2: 前端类型检查**

Run: `npx vue-tsc --noEmit`
Expected: 无输出（通过）

- [ ] **Step 3: 实机走查（可选但推荐）**

启动 `npm run tauri:dev`，执行一次安装/启停操作后进入「操作记录」页，确认：
- 概览卡有数字；
- 表格出现刚执行的操作（action/target 正确）；
- 关键字过滤、时间范围、操作类型下拉生效；
- 导出 CSV 内容与表格一致。

- [ ] **Step 4: 提交（若走查中有微调）**

```bash
git add -A
git commit -m "chore(audit): 实机走查微调"
```

---

## Self-Review

**Spec coverage：**
- 存储 JSONL + 保留 7 天 → Task 1 / Task 5
- `oplog!` 宏落点、28 调用点零改动 → Task 5
- `list_audit_entries` / `audit_stats` / `export_audit_entries` → Task 6
- 查询过滤（天/action/关键字）与截断 → Task 2
- 统计（今日/总数/Top/最近）→ Task 3
- CSV 导出（转义）→ Task 4
- 前端页面（概览/过滤/表格/导出）+ 路由 + 侧边栏 + i18n → Task 7 / Task 8
- 测试（record/query/stats/csv/cleanup）→ Task 1–5
- 二期（result/error）明确不做 → 未安排任务，符合 spec

**Placeholder scan：** 无 TBD/TODO；每个代码步骤均含完整代码。

**Type consistency：**
- `AuditEntry.ts/action/target/detail`、`AuditQuery.entries/truncated`、`ActionCount.action/count`、`AuditStats.today/total/by_action/last_ts` 在 Rust（Task 1/2/3）与 TS（Task 7）两处命名一致。
- 命令名 `list_audit_entries` / `audit_stats` / `export_audit_entries` 在 Task 6 定义、Task 7 调用一致；`export_audit_entries` 的 `dest_path` 在 JS 侧按 Tauri 约定传 `destPath`。
