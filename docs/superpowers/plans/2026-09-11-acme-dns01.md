# 网站证书自动化（ACME / Let's Encrypt，DNS-01）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为网站一键签发 Let's Encrypt 证书（DNS-01 + Cloudflare），并自动续期。

**Architecture:** 新增 `services/acme/`：`dns`（服务商 trait + Cloudflare 实现）、`mod`（`issue_certificate` 编排）、`renew_scheduler`（常驻续期循环）。`SslConfig` 增 `acme`/`cert_expires_at`；新增命令 `issue_site_certificate`；设置增 DNS 服务商字段；前端站点对话框 + 设置页接入。

**Tech Stack:** Rust（tauri v2, tokio, reqwest 0.13, sha2, chrono, serde_json）新增 `instant-acme`、`base64`；Vue 3 + TS。

## Global Constraints

- 分支：`feat/acme-dns01`（已创建；spec 已提交 `873b8ca`）。
- 新增依赖仅允许：`instant-acme`、`base64`。其余复用现有（`reqwest` json、`sha2`、`tokio`、`chrono`、`serde_json`）。
- DNS-01 only；v1 仅 Cloudflare；`server_name` 精确域名（不做通配符）。
- 凭据存 `settings.json` 明文（UI 需风险提示）。
- ACME 账户凭据：`crate::utils::paths::data_dir().join("acme-account.json")`。
- 证书落盘：`<nginx>/sites-data/certs/<domain>.crt` / `.key`（与既有自签实现同目录规则）。
- 续期阈值 30 天；调度循环每小时一次。
- 后端验证：`cd src-tauri && cargo test --lib`；前端 `npx vue-tsc --noEmit`。
- i18n 键必须同时加 `src/locales/zh-CN.ts` 与 `src/locales/en-US.ts`。
- 提交信息中文；不加 Co-Authored-By。

---

### Task 1: 依赖 + DNS 服务商抽象 + Cloudflare 实现

**Files:**
- Modify: `src-tauri/Cargo.toml`
- Create: `src-tauri/src/services/acme/dns/mod.rs`
- Create: `src-tauri/src/services/acme/dns/cloudflare.rs`
- Modify: `src-tauri/src/services/mod.rs`（注册 `pub mod acme;`）与 `src-tauri/src/services/acme/mod.rs`（本任务先建空模块文件，Task 2 填充）

**Interfaces:**
- Produces:
  - `trait DnsProvider { fn id(&self) -> &str; async fn find_zone(&self, domain: &str) -> anyhow::Result<String>; async fn create_txt(&self, fqdn: &str, value: &str) -> anyhow::Result<()>; async fn delete_txt(&self, fqdn: &str, value: &str) -> anyhow::Result<()>; }`
  - `pub fn provider_for(id: &str, token: &str) -> Option<Box<dyn DnsProvider>>`
  - `pub fn longest_zone_match(domain: &str, zones: &[String]) -> Option<String>`
  - `pub fn acme_challenge_fqdn(domain: &str) -> String`

- [ ] **Step 1: 写失败的测试**

创建 `src-tauri/src/services/acme/dns/mod.rs`，先只写测试与 `use`：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn longest_zone_match_picks_most_specific_suffix() {
        let zones = vec!["example.com".to_string(), "b.example.com".to_string(), "other.org".to_string()];
        assert_eq!(longest_zone_match("a.b.example.com", &zones).as_deref(), Some("b.example.com"));
        assert_eq!(longest_zone_match("x.example.com", &zones).as_deref(), Some("example.com"));
        assert_eq!(longest_zone_match("nope.net", &zones), None);
    }

    #[test]
    fn acme_challenge_fqdn_prefixes_label() {
        assert_eq!(acme_challenge_fqdn("example.com"), "_acme-challenge.example.com");
        assert_eq!(acme_challenge_fqdn("a.b.example.com"), "_acme-challenge.a.b.example.com");
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd src-tauri && cargo test --lib acme::dns::`
Expected: 编译失败（函数未定义）

- [ ] **Step 3: 实现 trait 与纯函数**

`src-tauri/src/services/acme/dns/mod.rs`：

```rust
//! DNS 服务商抽象（DNS-01 用）。v1 仅 Cloudflare，新增服务商只需加一个实现文件并在
//! `provider_for` 注册。
//!
//! 注意：需以 `Box<dyn DnsProvider>` 使用，而 trait 对象不支持 `async fn`（RPITIT 非
//! dyn 兼容），故统一返回 boxed future。

pub mod cloudflare;

use std::future::Future;
use std::pin::Pin;

use anyhow::Result;

pub type BoxFuture<'a, T> = Pin<Box<dyn Future<Output = T> + Send + 'a>>;

pub trait DnsProvider: Send + Sync {
    fn id(&self) -> &str;
    /// 返回 domain 所属 zone（注册域名），用于拼接记录全名。
    fn find_zone<'a>(&'a self, domain: &'a str) -> BoxFuture<'a, Result<String>>;
    fn create_txt<'a>(&'a self, fqdn: &'a str, value: &'a str) -> BoxFuture<'a, Result<()>>;
    fn delete_txt<'a>(&'a self, fqdn: &'a str, value: &'a str) -> BoxFuture<'a, Result<()>>;
}

/// 按 id 取服务商实现。
pub fn provider_for(id: &str, token: &str) -> Option<Box<dyn DnsProvider>> {
    match id {
        "cloudflare" => Some(Box::new(cloudflare::Cloudflare::new(token.to_string()))),
        _ => None,
    }
}

/// zones 中与 domain 匹配（相等或以 `.` 结尾后缀）的最长 zone。
pub fn longest_zone_match(domain: &str, zones: &[String]) -> Option<String> {
    let d = domain.trim_end_matches('.').to_lowercase();
    zones
        .iter()
        .filter(|z| {
            let zl = z.trim_end_matches('.').to_lowercase();
            d == zl || d.ends_with(&format!(".{zl}"))
        })
        .max_by_key(|z| z.len())
        .cloned()
}

/// DNS-01 记录全名。
pub fn acme_challenge_fqdn(domain: &str) -> String {
    format!("_acme-challenge.{}", domain.trim_end_matches('.'))
}
```

- [ ] **Step 4: 实现 Cloudflare**

`src-tauri/src/services/acme/dns/cloudflare.rs`——trait 方法须返回 `BoxFuture`，故每个 `impl DnsProvider` 方法体写成 `Box::pin(async move { ... })`（内部辅助方法用普通 `async fn` 即可）：

```rust
//! Cloudflare DNS API（Bearer Token）实现 DNS-01 的 TXT 记录增删。

use anyhow::{anyhow, Result};
use serde_json::Value;

use super::{longest_zone_match, DnsProvider};

const API: &str = "https://api.cloudflare.com/client/v4";
/// DNS 传播等待：新建记录后固定等待，随后由 ACME 轮询兜底。
pub const DNS_PROPAGATION_WAIT_SECS: u64 = 15;

pub struct Cloudflare {
    token: String,
    client: reqwest::Client,
}

impl Cloudflare {
    pub fn new(token: String) -> Self {
        Self { token, client: reqwest::Client::new() }
    }

    async fn get(&self, url: &str) -> Result<Value> {
        let resp = self.client.get(url).bearer_auth(&self.token).send().await?;
        Self::json(resp).await
    }

    async fn json(resp: reqwest::Response) -> Result<Value> {
        let status = resp.status();
        let body: Value = resp.json().await.unwrap_or(Value::Null);
        if !status.is_success() || body.get("success").and_then(|v| v.as_bool()) != Some(true) {
            return Err(anyhow!("Cloudflare API 失败（{}）：{}", status, body));
        }
        Ok(body)
    }
}

impl DnsProvider for Cloudflare {
    fn id(&self) -> &str {
        "cloudflare"
    }

    async fn find_zone(&self, domain: &str) -> Result<String> {
        let body = self.get(&format!("{}/zones?per_page=50", API)).await?;
        let names: Vec<String> = body["result"]
            .as_array()
            .map(|arr| {
                arr.iter()
                    .filter_map(|z| z["name"].as_str().map(|s| s.to_string()))
                    .collect()
            })
            .unwrap_or_default();
        longest_zone_match(domain, &names)
            .ok_or_else(|| anyhow!("该域名不在 Cloudflare 账户的 zone 中：{}", domain))
    }

    async fn create_txt(&self, fqdn: &str, value: &str) -> Result<()> {
        let zone = self.find_zone(fqdn.trim_start_matches("_acme-challenge.")).await?;
        let zone_id = self.zone_id(&zone).await?;
        let body = serde_json::json!({ "type": "TXT", "name": fqdn, "content": value, "ttl": 120 });
        let resp = self
            .client
            .post(format!("{}/zones/{}/dns_records", API, zone_id))
            .bearer_auth(&self.token)
            .json(&body)
            .send()
            .await?;
        Self::json(resp).await.map(|_| ())
    }

    async fn delete_txt(&self, fqdn: &str, value: &str) -> Result<()> {
        let zone = self.find_zone(fqdn.trim_start_matches("_acme-challenge.")).await?;
        let zone_id = self.zone_id(&zone).await?;
        let body = self
            .get(&format!(
                "{}/zones/{}/dns_records?type=TXT&name={}",
                API, zone_id, fqdn
            ))
            .await?;
        if let Some(records) = body["result"].as_array() {
            for r in records {
                if r["content"].as_str() == Some(value) {
                    if let Some(id) = r["id"].as_str() {
                        let _ = self
                            .client
                            .delete(format!("{}/zones/{}/dns_records/{}", API, zone_id, id))
                            .bearer_auth(&self.token)
                            .send()
                            .await;
                    }
                }
            }
        }
        Ok(())
    }
}

impl Cloudflare {
    async fn zone_id(&self, zone: &str) -> Result<String> {
        let body = self
            .get(&format!("{}/zones?name={}", API, zone))
            .await?;
        body["result"]
            .as_array()
            .and_then(|a| a.first())
            .and_then(|z| z["id"].as_str())
            .map(|s| s.to_string())
            .ok_or_else(|| anyhow!("未找到 zone id：{}", zone))
    }
}
```

注：`zone_id` 为内部辅助方法，普通 `async fn` 即可；trait 方法保持 `Box::pin` 包装。

- [ ] **Step 5: 注册模块并编译测试**

`src-tauri/src/services/mod.rs` 加 `pub mod acme;`；创建 `src-tauri/src/services/acme/mod.rs`（本任务仅 `pub mod dns;`）。

在 `src-tauri/Cargo.toml` 的 `[dependencies]` 加：

```toml
instant-acme = "0.8"
base64 = "0.22"
```

Run: `cd src-tauri && cargo test --lib acme::dns::`
Expected: PASS（2 个测试）

- [ ] **Step 6: 提交**

```bash
git add src-tauri/Cargo.toml src-tauri/src/services/mod.rs src-tauri/src/services/acme/
git commit -m "feat(acme): DNS 服务商抽象 + Cloudflare TXT 增删"
```

---

### Task 2: ACME 签发核心

**Files:**
- Modify: `src-tauri/src/services/acme/mod.rs`
- Test: `src-tauri/src/services/acme/mod.rs`（内联）

**Interfaces:**
- Consumes: `dns::{provider_for, acme_challenge_fqdn, DnsProvider}`、`dns::cloudflare::DNS_PROPAGATION_WAIT_SECS`
- Produces:
  - `pub struct AcmeSettings { pub dns_provider: String, pub cloudflare_api_token: String, pub use_staging: bool }`
  - `pub async fn issue_certificate(domain: &str, settings: &AcmeSettings, cert_dir: &std::path::Path, on_progress: impl Fn(&str, &str) + Send + Sync) -> anyhow::Result<(String, String)>`（返回 `(cert_path, key_path)`）
  - `pub fn account_path() -> std::path::PathBuf`

- [ ] **Step 1: 写失败测试（纯逻辑部分）**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn directory_url_switches_on_staging() {
        assert!(directory_url(true).contains("staging"));
        assert!(!directory_url(false).contains("staging"));
    }

    #[test]
    fn cert_paths_use_domain() {
        let dir = std::path::Path::new("C:/tmp/certs");
        let (c, k) = cert_paths(dir, "example.com");
        assert!(c.ends_with("example.com.crt"));
        assert!(k.ends_with("example.com.key"));
    }
}
```

- [ ] **Step 2: 运行确认失败**

Run: `cd src-tauri && cargo test --lib acme::tests`
Expected: 编译失败

- [ ] **Step 3: 实现**

`src-tauri/src/services/acme/mod.rs`：

```rust
//! ACME（Let's Encrypt）DNS-01 签发与续期。

pub mod dns;
pub mod renew_scheduler;

use std::path::{Path, PathBuf};
use std::time::Duration;

use anyhow::{anyhow, Context, Result};
use instant_acme::{
    Account, ChallengeType, Identifier, LetsEncrypt, NewAccount, NewOrder, RetryPolicy,
};

use dns::provider_for;

pub struct AcmeSettings {
    pub dns_provider: String,
    pub cloudflare_api_token: String,
    pub use_staging: bool,
}

pub fn account_path() -> PathBuf {
    crate::utils::paths::data_dir().join("acme-account.json")
}

pub fn directory_url(staging: bool) -> String {
    if staging {
        LetsEncrypt::Staging.url().to_string()
    } else {
        LetsEncrypt::Production.url().to_string()
    }
}

pub fn cert_paths(dir: &Path, domain: &str) -> (PathBuf, PathBuf) {
    (dir.join(format!("{}.crt", domain)), dir.join(format!("{}.key", domain)))
}

/// 签发（或重新签发）指定域名的证书，返回 (cert_path, key_path)。
pub async fn issue_certificate(
    domain: &str,
    settings: &AcmeSettings,
    cert_dir: &Path,
    on_progress: impl Fn(&str, &str),
) -> Result<(PathBuf, PathBuf)> {
    let provider = provider_for(&settings.dns_provider, &settings.cloudflare_api_token)
        .ok_or_else(|| anyhow!("未配置 DNS 服务商或服务商不支持：{}", settings.dns_provider))?;

    std::fs::create_dir_all(cert_dir).context("创建证书目录失败")?;

    on_progress("creating-order", "创建 ACME 订单");
    let account = load_or_create_account(settings.use_staging).await?;
    let mut order = account
        .new_order(&NewOrder::new(&[Identifier::Dns(domain.to_string())]))
        .await?;

    let mut authorizations = order.authorizations();
    while let Some(result) = authorizations.next().await {
        let mut authz = result?;
        let Some(mut challenge) = authz.challenge(ChallengeType::Dns01) else {
            return Err(anyhow!("该 ACME 服务器未提供 DNS-01 challenge"));
        };
        let value = challenge.key_authorization()?.dns_value();
        let fqdn = dns::acme_challenge_fqdn(domain);
        on_progress("waiting-dns", &format!("写入 TXT 记录 {}", fqdn));
        provider.create_txt(&fqdn, &value).await?;
        // 传播等待；ACME 轮询会重试兜底
        tokio::time::sleep(Duration::from_secs(dns::cloudflare::DNS_PROPAGATION_WAIT_SECS)).await;
        on_progress("validating", "等待 ACME 验证");
        if let Err(e) = challenge.set_ready().await {
            let _ = provider.delete_txt(&fqdn, &value).await;
            return Err(e).context("提交 DNS-01 challenge 失败");
        }
        // 验证完成后清理 TXT（尽力）
        let _ = provider.delete_txt(&fqdn, &value).await;
    }

    order.poll_ready(&RetryPolicy::default()).await?;
    on_progress("downloading", "签发完成，下载证书");
    let key_pem = order.finalize().await?;
    let cert_pem = order.poll_certificate(&RetryPolicy::default()).await?;

    let (cert_path, key_path) = cert_paths(cert_dir, domain);
    std::fs::write(&cert_path, cert_pem).context("写入证书失败")?;
    std::fs::write(&key_path, key_pem).context("写入私钥失败")?;
    on_progress("done", "证书已保存");
    Ok((cert_path, key_path))
}

async fn load_or_create_account(staging: bool) -> Result<Account> {
    let dir_url = directory_url(staging);
    let path = account_path();
    if let Ok(raw) = std::fs::read_to_string(&path) {
        if let Ok(creds) = serde_json::from_str(&raw) {
            return Ok(Account::builder()?.from_credentials(creds).await?);
        }
    }
    let (account, creds) = Account::builder()?
        .create(
            &NewAccount {
                contact: &[],
                terms_of_service_agreed: true,
                only_return_existing: false,
            },
            dir_url,
            None,
        )
        .await?;
    if let Ok(json) = serde_json::to_string_pretty(&creds) {
        let _ = std::fs::write(&path, json);
    }
    Ok(account)
}
```

> 实现注意：`instant-acme` 的 `Account::builder()` / `new_order` / `authorizations()` / `set_ready` / `poll_ready` / `finalize` / `poll_certificate` 的确切签名以 crate 文档为准（本计划基于 `/djc/instant-acme` 文档）；`AccountCredentials` 类型用于 `from_credentials` 的反序列化。若 API 版本差异导致编译失败，按编译器提示与 crate 文档调整（不改设计）。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd src-tauri && cargo test --lib acme::`
Expected: PASS（dns + acme 测试）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/services/acme/mod.rs
git commit -m "feat(acme): DNS-01 签发核心（订单/验证/下载）"
```

---

### Task 3: 模型字段与站点保存接线

**Files:**
- Modify: `src-tauri/src/models/website.rs`（`SslConfig`）
- Modify: `src/models/website.ts`
- Test: `src-tauri/src/models/website.rs`（内联）

**Interfaces:**
- Produces: `SslConfig { enabled, cert_path, key_path, acme: bool, cert_expires_at: Option<String> }`

- [ ] **Step 1: 失败测试（旧数据兼容）**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn ssl_config_without_acme_fields_defaults() {
        let c: SslConfig = serde_json::from_str(r#"{"enabled":true,"cert_path":"a.crt","key_path":"a.key"}"#).unwrap();
        assert!(c.enabled);
        assert!(!c.acme);
        assert!(c.cert_expires_at.is_none());
    }
}
```

- [ ] **Step 2: 确认失败**

Run: `cd src-tauri && cargo test --lib website::`
Expected: 编译失败（字段不存在）

- [ ] **Step 3: 加字段**

`SslConfig` 增：

```rust
    /// 是否由 ACME（Let's Encrypt）签发
    #[serde(default)]
    pub acme: bool,
    /// ACME 证书到期时间（RFC3339 本地时间）；自签为空
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cert_expires_at: Option<String>,
```

前端 `src/models/website.ts` 的 `SslConfig` 同步加 `acme: boolean`、`cert_expires_at?: string | null`。

- [ ] **Step 4: 确认通过 + 提交**

Run: `cd src-tauri && cargo test --lib website::` → PASS

```bash
git add src-tauri/src/models/website.rs src/models/website.ts
git commit -m "feat(acme): SslConfig 增 acme 与到期时间字段"
```

---

### Task 4: 命令 `issue_site_certificate` + 注册

> **前置**：本任务依赖 Task 6 的 `crate::commands::config::read_settings()`，执行顺序上 **Task 6 需先于本任务**。
> **同时需要**：把 `src-tauri/src/commands/website.rs` 中既有的 `fn resolve_nginx` 与 `fn regenerate` 改为 `pub fn`（本任务与 Task 5 从其它模块调用它们）。

**Files:**
- Modify: `src-tauri/src/commands/website.rs`
- Modify: `src-tauri/src/lib.rs`（注册命令）

**Interfaces:**
- Consumes: `services::acme::{issue_certificate, AcmeSettings}`、`paths::data_dir()`、`commands::website::{resolve_nginx, regenerate}`、`utils::paths::resolve_install_path`
- Produces: `#[tauri::command] pub async fn issue_site_certificate(app: AppHandle, wm: State<Arc<WebsiteManager>>, sm: State<Arc<SoftwareManager>>, site_id: String) -> Result<(), String>`

- [ ] **Step 1: 实现命令**

在 `commands/website.rs` 追加：

```rust
/// 为站点申请/重新申请 Let's Encrypt 证书（DNS-01）。
#[tauri::command]
pub async fn issue_site_certificate(
    app: tauri::AppHandle,
    wm: State<'_, Arc<WebsiteManager>>,
    sm: State<'_, Arc<SoftwareManager>>,
    site_id: String,
) -> Result<(), String> {
    use tauri::Emitter;
    let site = wm.get(&site_id).ok_or_else(|| format!("未找到站点: {}", site_id))?;
    let domain = site
        .server_name
        .clone()
        .filter(|s| !s.trim().is_empty())
        .ok_or_else(|| "请先填写 server_name（域名）".to_string())?;

    let settings = crate::commands::config::read_settings()?; // 见 Task 6；此处读 DNS 相关字段
    let acme_settings = crate::services::acme::AcmeSettings {
        dns_provider: settings.dns_provider.clone(),
        cloudflare_api_token: settings.cloudflare_api_token.clone(),
        use_staging: settings.acme_use_staging,
    };

    let nginx = resolve_nginx(&sm)?;
    let cert_dir = PathBuf::from(&nginx.install_path).join("sites-data").join("certs");

    let app_for_progress = app.clone();
    let domain_for_progress = domain.clone();
    let on_progress = move |phase: &str, msg: &str| {
        let _ = app_for_progress.emit(
            "acme-progress",
            serde_json::json!({ "domain": domain_for_progress, "phase": phase, "message": msg }),
        );
    };

    let (cert_path, key_path) = crate::services::acme::issue_certificate(
        &domain,
        &acme_settings,
        &cert_dir,
        on_progress,
    )
    .await
    .map_err(|e| e.to_string())?;

    // 更新站点 ssl 并落盘 + 重新生成 nginx 配置并 reload
    let mut updated = site.clone();
    updated.ssl.enabled = true;
    updated.ssl.acme = true;
    updated.ssl.cert_path = Some(cert_path.to_string_lossy().to_string());
    updated.ssl.key_path = Some(key_path.to_string_lossy().to_string());
    updated.ssl.cert_expires_at = Some(
        (chrono::Local::now() + chrono::Duration::days(90)).to_rfc3339(),
    );
    wm.upsert_mem(updated)?;
    wm.persist().map_err(|e| e.to_string())?;
    regenerate(&sm, &wm, true)?;

    crate::oplog!("acme_issue", &format!("{} ({})", site.name, domain));
    Ok(())
}
```

`lib.rs` 的 `generate_handler!` 注册 `commands::website::issue_site_certificate`。

> 注：`crate::commands::config::read_settings()` 需在 Task 6 落地；若先做本任务，可临时直接读 `AppSettings` 文件（`paths::settings_path()`），Task 6 再统一。

- [ ] **Step 2: 编译**

Run: `cd src-tauri && cargo check --lib`
Expected: Finished（Task 6 未完成时，若 `read_settings` 不存在，先按注释临时实现读 settings.json）

- [ ] **Step 3: 提交**

```bash
git add src-tauri/src/commands/website.rs src-tauri/src/lib.rs
git commit -m "feat(acme): issue_site_certificate 命令"
```

---

### Task 5: 续期调度器

> **前置**：依赖 Task 6 的 `read_settings()` 与 Task 4 对 `resolve_nginx`/`regenerate` 的 `pub` 化。

**Files:**
- Create: `src-tauri/src/services/acme/renew_scheduler.rs`
- Modify: `src-tauri/src/lib.rs`（启动循环）

**Interfaces:**
- Consumes: `services::acme::{issue_certificate, AcmeSettings}`、`WebsiteManager::{list, get, upsert_mem, persist}`、`commands::website::{resolve_nginx, regenerate}`
- Produces: `pub fn needs_renewal(expires: Option<&str>, now: chrono::DateTime<chrono::Local>, days: i64) -> bool`；`pub async fn run_scheduler(app: AppHandle, wm: Arc<WebsiteManager>, sm: Arc<SoftwareManager>)`

- [ ] **Step 1: 失败测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use chrono::TimeZone;

    fn now() -> chrono::DateTime<chrono::Local> {
        chrono::Local.with_ymd_and_hms(2026, 9, 11, 0, 0, 0).unwrap()
    }

    #[test]
    fn needs_renewal_rules() {
        // 缺失/不可解析 → true（无从确认，重签自愈）
        assert!(needs_renewal(None, now(), 30));
        assert!(needs_renewal(Some("not-a-date"), now(), 30));
        // 已过期 → true
        let past = (now() - chrono::Duration::days(1)).to_rfc3339();
        assert!(needs_renewal(Some(&past), now(), 30));
        // 距到期 <30 天 → true
        let soon = (now() + chrono::Duration::days(10)).to_rfc3339();
        assert!(needs_renewal(Some(&soon), now(), 30));
        // 充足 → false
        let far = (now() + chrono::Duration::days(80)).to_rfc3339();
        assert!(!needs_renewal(Some(&far), now(), 30));
    }
}
```

- [ ] **Step 2: 确认失败**

Run: `cd src-tauri && cargo test --lib acme::renew_scheduler`
Expected: 编译失败

- [ ] **Step 3: 实现**

```rust
//! ACME 证书自动续期：每小时检查一次，距到期 <30 天则续期。

use std::sync::Arc;
use std::time::Duration;

use tauri::AppHandle;

use crate::services::software_manager::SoftwareManager;
use crate::services::website_manager::WebsiteManager;

const CHECK_INTERVAL_SECS: u64 = 3600;
const RENEW_BEFORE_DAYS: i64 = 30;

/// 是否需要续期：缺失/不可解析 → true；已过期或距到期 < days → true。
pub fn needs_renewal(expires: Option<&str>, now: chrono::DateTime<chrono::Local>, days: i64) -> bool {
    let Some(s) = expires else { return true };
    let Ok(exp) = chrono::DateTime::parse_from_rfc3339(s) else { return true };
    exp.with_timezone(&chrono::Local) - now < chrono::Duration::days(days)
}

pub async fn run_scheduler(app: AppHandle, wm: Arc<WebsiteManager>, sm: Arc<SoftwareManager>) {
    let mut tick = tokio::time::interval(Duration::from_secs(CHECK_INTERVAL_SECS));
    tick.tick().await; // 消耗初始化 tick
    loop {
        tick.tick().await;
        let settings = match crate::commands::config::read_settings() {
            Ok(s) => s,
            Err(_) => continue,
        };
        let acme = crate::services::acme::AcmeSettings {
            dns_provider: settings.dns_provider.clone(),
            cloudflare_api_token: settings.cloudflare_api_token.clone(),
            use_staging: settings.acme_use_staging,
        };
        for site in wm.list().into_iter().filter(|s| s.ssl.acme) {
            if !needs_renewal(site.ssl.cert_expires_at.as_deref(), chrono::Local::now(), RENEW_BEFORE_DAYS) {
                continue;
            }
            let Some(domain) = site.server_name.clone().filter(|d| !d.trim().is_empty()) else {
                continue;
            };
            let Ok(nginx) = crate::commands::website::resolve_nginx(&sm) else { continue };
            let cert_dir = std::path::PathBuf::from(&nginx.install_path).join("sites-data").join("certs");
            match crate::services::acme::issue_certificate(&domain, &acme, &cert_dir, |_, _| {}).await {
                Ok((cert, key)) => {
                    if let Some(mut s) = wm.get(&site.id) {
                        s.ssl.cert_path = Some(cert.to_string_lossy().to_string());
                        s.ssl.key_path = Some(key.to_string_lossy().to_string());
                        s.ssl.cert_expires_at = Some((chrono::Local::now() + chrono::Duration::days(90)).to_rfc3339());
                        let _ = wm.upsert_mem(s);
                        let _ = wm.persist();
                    }
                    let _ = crate::commands::website::regenerate(&sm, &wm, true);
                    crate::oplog!("acme_renew", &format!("{} ({})", site.name, domain));
                }
                Err(e) => {
                    tracing::warn!(site = %site.id, error = %e, "ACME 自动续期失败");
                }
            }
        }
        let _ = &app;
    }
}
```

`lib.rs` setup 中 spawn：

```rust
            let renew_wm = app.state::<std::sync::Arc<crate::services::website_manager::WebsiteManager>>().inner().clone();
            let renew_sm = sw_mgr_arc_for_renew.clone(); // 从 SoftwareManager state 取 Arc
            let renew_app = app.handle().clone();
            tauri::async_runtime::spawn(async move {
                crate::services::acme::renew_scheduler::run_scheduler(renew_app, renew_wm, renew_sm).await;
            });
```

（`sw_mgr_arc_for_renew` 按 `lib.rs` 既有取 State Arc 的写法获取。）

- [ ] **Step 4: 测试通过 + 提交**

Run: `cd src-tauri && cargo test --lib acme::renew_scheduler` → PASS

```bash
git add src-tauri/src/services/acme/renew_scheduler.rs src-tauri/src/lib.rs
git commit -m "feat(acme): 证书自动续期调度（距到期 <30 天）"
```

---

### Task 6: 设置字段与读取辅助

**Files:**
- Modify: `src-tauri/src/models/settings.rs`
- Modify: `src-tauri/src/commands/config.rs`
- Modify: `src/models/settings.ts`

**Interfaces:**
- Produces: `AppSettings { dns_provider: String, cloudflare_api_token: String, acme_use_staging: bool }`；`pub fn read_settings() -> Result<AppSettings, String>`

- [ ] **Step 1: 加字段与 Default**

`AppSettings` 增（并更新 `Default`）：

```rust
    /// DNS 服务商（DNS-01 用），v1 支持 "cloudflare"
    #[serde(default = "default_dns_provider")]
    pub dns_provider: String,
    /// DNS 服务商 API Token（明文存储，UI 需提示风险）
    #[serde(default)]
    pub cloudflare_api_token: String,
    /// 使用 Let's Encrypt 测试环境（staging）
    #[serde(default)]
    pub acme_use_staging: bool,
```

```rust
fn default_dns_provider() -> String { "cloudflare".to_string() }
```

`Default` 实现中补 `dns_provider: default_dns_provider(), cloudflare_api_token: String::new(), acme_use_staging: false`。

- [ ] **Step 2: 读取辅助**

`commands/config.rs` 增：

```rust
/// 从 settings.json 读取设置（失败返回默认值）。
pub fn read_settings() -> Result<crate::models::settings::AppSettings, String> {
    let path = crate::utils::paths::settings_path();
    match std::fs::read_to_string(&path) {
        Ok(s) => serde_json::from_str(&s).map_err(|e| e.to_string()),
        Err(_) => Ok(Default::default()),
    }
}
```

前端 `src/models/settings.ts` 的 `AppSettings` 同步加三个字段。

- [ ] **Step 3: 编译 + 提交**

Run: `cd src-tauri && cargo check --lib` → Finished

```bash
git add src-tauri/src/models/settings.rs src-tauri/src/commands/config.rs src/models/settings.ts
git commit -m "feat(acme): 设置增 DNS 服务商与 staging 开关"
```

---

### Task 7: 前端站点对话框（证书来源 + 申请 + 进度）

**Files:**
- Modify: `src/modules/website-manager/components/SiteEditDialog.vue`
- Modify: `src/locales/zh-CN.ts`、`src/locales/en-US.ts`

**Interfaces:**
- Consumes: 命令 `issue_site_certificate`；事件 `acme-progress { domain, phase, message }`；`Site.ssl.{acme,cert_expires_at}`

- [ ] **Step 1: SSL 区加来源选择与申请按钮**

在 `SiteEditDialog.vue` 的 SSL 配置区加：

- 证书来源下拉：`self-signed` / `acme`（v-model 绑本地 `certSource`，初值由 `site.ssl.acme` 推导）
- ACME 分支显示：`申请证书` 按钮、`acmeStatus` 文本、`site.ssl.cert_expires_at` 到期时间
- 点击申请：

```ts
async function issueCert() {
  acmeBusy.value = true
  acmeStatus.value = t('acmeIssuing')
  try {
    await invoke('issue_site_certificate', { siteId: props.site.id })
    acmeStatus.value = t('acmeDone')
  } catch (e) {
    acmeStatus.value = String(e)
  } finally {
    acmeBusy.value = false
  }
}
```

- `onMounted` 监听 `acme-progress`（`e.payload.domain === site.server_name` 时更新 `acmeStatus`），`onUnmounted` 注销。
- 保存时把 `ssl.acme` 按 `certSource === 'acme'` 写入。

- [ ] **Step 2: i18n**

`zh-CN.ts`：`certSource: '证书来源'`、`certSelfSigned: '自签证书'`、`certAcme: 'Let\'s Encrypt'`、`issueCert: '申请证书'`、`reissueCert: '重新申请'`、`acmeIssuing: '正在申请证书…'`、`acmeDone: '证书已签发'`、`certExpiresAt: '到期时间'`、`acmeNeedToken: '请先在设置中配置 DNS 服务商 Token'`。
`en-US.ts` 对应英文。

- [ ] **Step 3: 类型检查 + 提交**

Run: `npx vue-tsc --noEmit` → 无输出

```bash
git add src/modules/website-manager/components/SiteEditDialog.vue src/locales/zh-CN.ts src/locales/en-US.ts
git commit -m "feat(acme): 站点对话框证书来源与申请入口"
```

---

### Task 8: 前端设置页（DNS 服务商区）

**Files:**
- Modify: `src/modules/settings/pages/SettingsPage.vue`
- Modify: `src/locales/zh-CN.ts`、`src/locales/en-US.ts`

- [ ] **Step 1: 新增区块**

设置页加「DNS 服务商（证书自动化）」区：
- 服务商下拉（仅 `cloudflare`）
- API Token：`type="password"` 输入，绑定 `settings.cloudflare_api_token`
- 明文风险提示文案
- 「使用 Let's Encrypt 测试环境（staging）」开关
- 随既有设置保存流程提交

- [ ] **Step 2: i18n**

`zh-CN.ts`：`dnsProvider: 'DNS 服务商'`、`dnsProviderDesc: '用于自动申请/续期 HTTPS 证书（DNS-01）'`、`cloudflareToken: 'Cloudflare API Token'`、`dnsTokenPlaintextWarning: 'Token 以明文保存在配置文件中，请确保本机环境可信'`、`acmeStaging: '使用 Let\'s Encrypt 测试环境'`。`en-US.ts` 对应英文。

- [ ] **Step 3: 类型检查 + 提交**

Run: `npx vue-tsc --noEmit` → 无输出

```bash
git add src/modules/settings/pages/SettingsPage.vue src/locales/zh-CN.ts src/locales/en-US.ts
git commit -m "feat(acme): 设置页 DNS 服务商与 Token 配置"
```

---

### Task 9: 全量验证

- [ ] **Step 1: 后端全量测试**

Run: `cd src-tauri && cargo test --lib`
Expected: 全绿（既有 131 + 新增约 6）

- [ ] **Step 2: 前端类型检查**

Run: `npx vue-tsc --noEmit`
Expected: 无输出

- [ ] **Step 3: 实机走查（需真实 Cloudflare Token 与域名）**

1. 设置页填 Cloudflare Token（staging 打开）→ 站点 server_name 填 Cloudflare 托管的域名 → 「申请证书」
2. 观察进度文本推进（creating-order → waiting-dns → validating → downloading → done）
3. `https://<域名>` 证书链有效（staging 证书浏览器会告警，属正常）；`nginx -t` 通过
4. 站点对话框显示到期时间；把 `cert_expires_at` 手改为 1 天后重启应用 → 续期循环应重新签发（可临时把 `CHECK_INTERVAL_SECS` 调小验证）
5. 审计页应出现 `acme_issue` / `acme_renew`

- [ ] **Step 4: 提交（若走查有微调）**

```bash
git add -A && git commit -m "chore(acme): 实机走查微调"
```

---

## Self-Review

**Spec coverage：**
- 依赖 `instant-acme`/`base64` → Task 1
- DNS trait + Cloudflare（zone 最长后缀匹配、TXT 增删）→ Task 1
- 签发流程（订单→TXT→等待→set_ready→poll_ready→finalize→poll_certificate→落盘）→ Task 2
- 账户凭据 `acme-account.json` → Task 2
- `SslConfig.acme`/`cert_expires_at` + 旧数据兼容 → Task 3
- 命令 `issue_site_certificate` + 前端进度事件 → Task 4 / Task 7
- 自动续期（每小时、<30 天、`needs_renewal`）→ Task 5
- 设置（provider/token/staging + 明文风险提示）→ Task 6 / Task 8
- 边界（不做 HTTP-01/通配符/其它 DNS 商/keyring/吊销）→ 未安排任务，符合 spec

**Placeholder scan：** 无 TBD/TODO。Task 2/4/5 中「以 crate 文档为准/按编译器提示」属 API 版本适配说明，非占位实现；其余步骤含完整代码。

**Type consistency：**
- `AcmeSettings{dns_provider,cloudflare_api_token,use_staging}` 在 Task 2 定义，Task 4/5 使用一致。
- `issue_certificate(domain, &AcmeSettings, cert_dir, on_progress) -> (cert_path, key_path)` 在 Task 2 定义，Task 4/5 调用一致。
- `needs_renewal(Option<&str>, DateTime<Local>, i64) -> bool` Task 5 内部一致。
- `provider_for(id, token) -> Option<Box<dyn DnsProvider>>`、`acme_challenge_fqdn`、`longest_zone_match` Task 1 定义、Task 2 使用一致。
