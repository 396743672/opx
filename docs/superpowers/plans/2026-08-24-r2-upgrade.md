# R2 软件升级检测 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 已装软件与 catalog 版本对比，检测到更高版本时在已装列表行内显示「可升级」，一键直装新版（新旧并存、不自动启动）。

**Architecture:** 后端加纯函数 `compare_versions` + `compute_upgrades`，暴露 `check_upgrades` 命令（纯内置 catalog 对比，立即返回）；前端列表加载时先显示检测结果，再并行调已有 `fetch_remote_versions_for` 在线刷新（失败静默回退）。升级复用 `install_software`（新目录新记录）+ 全局 `installStore` 进度面板。

**Tech Stack:** Rust (tauri command + serde)、Vue 3 Composition API、Pinia (`useInstallStore`)。

## Global Constraints

- 所有代码在 dev 分支上开发（本仓库规则：禁止直接改 master）。
- 目标版本 = catalog 中「高于当前版本的最高版本」，允许跨大版本。
- 升级只安装**不自动启动**；新旧并存，旧版记录/目录保留。
- 在线刷新走已有 `fetch_remote_versions_for` 命令，前端 `Promise.allSettled` 并发、失败静默。
- 版本比较规则：数字分段比较（`5.7.44 < 8.0.36`、`7.4.9 < 7.10.0`）；任一段非数字退化为字符串比较。

---

### Task 1: Rust `compare_versions` + 单测

**Files:**
- Modify: `src-tauri/src/commands/software.rs`（文件底部追加 `#[cfg(test)] mod tests` 与私有 `fn compare_versions`）
- Test: 同文件 `mod tests`

**Interfaces:**
- Produces: `fn compare_versions(a: &str, b: &str) -> std::cmp::Ordering` — 数字分段版本比较

- [ ] **Step 1: 在 `src-tauri/src/commands/software.rs` 添加测试与实现**

在文件末尾追加：

```rust
/// 数字分段版本比较：5.7.44 < 8.0.36；7.4.9 < 7.10.0；
/// 任一段含非数字时退化为字符串比较（v1 < v2）。
fn compare_versions(a: &str, b: &str) -> std::cmp::Ordering {
    use std::cmp::Ordering;
    let ap: Vec<&str> = a.split('.').collect();
    let bp: Vec<&str> = b.split('.').collect();
    for i in 0..ap.len().min(bp.len()) {
        match (ap[i].parse::<u64>(), bp[i].parse::<u64>()) {
            (Ok(x), Ok(y)) if x != y => return x.cmp(&y),
            (Ok(_), Ok(_)) => {}
            _ => return a.cmp(b),
        }
    }
    ap.len().cmp(&bp.len())
}

#[cfg(test)]
mod tests {
    use super::compare_versions;
    use std::cmp::Ordering;

    #[test]
    fn version_cross_major() {
        assert_eq!(compare_versions("5.7.44", "8.0.36"), Ordering::Less);
    }
    #[test]
    fn version_equal() {
        assert_eq!(compare_versions("8.0.36", "8.0.36"), Ordering::Equal);
    }
    #[test]
    fn version_prefix_longer_is_greater() {
        assert_eq!(compare_versions("1.31.2", "1.31"), Ordering::Greater);
    }
    #[test]
    fn version_numeric_segment() {
        assert_eq!(compare_versions("7.4.9", "7.10.0"), Ordering::Less);
    }
    #[test]
    fn version_non_numeric_falls_back_to_string() {
        assert_eq!(compare_versions("v1", "v2"), Ordering::Less);
    }
}
```

- [ ] **Step 2: 运行测试，确认全部通过**

Run: `cd /d/object/opx/src-tauri && cargo test --lib commands::software 2>&1 | tail -12`
Expected: `5 passed`（`version_cross_major` 等 5 个用例全部 ok）

- [ ] **Step 3: 提交**

```bash
git add src-tauri/src/commands/software.rs
git commit -m "feat(software): 增加版本比较函数与单测"
```

---

### Task 2: `UpgradeInfo` 模型 + `check_upgrades` 命令

**Files:**
- Modify: `src-tauri/src/models/software.rs`（追加 `UpgradeInfo`）
- Modify: `src-tauri/src/commands/software.rs`（追加 `compute_upgrades` + `check_upgrades` 命令）
- Modify: `src-tauri/src/lib.rs`（注册 `check_upgrades`）

**Interfaces:**
- Consumes: `compare_versions`（Task 1）
- Produces:
  - `pub struct UpgradeInfo { key: String, name: String, current_version: String, target_version: Option<String> }`
  - `fn compute_upgrades(installed: &[InstalledSoftware], catalog: &Catalog) -> Vec<UpgradeInfo>`
  - `#[tauri::command] pub async fn check_upgrades(manager: State<'_, Arc<SoftwareManager>>) -> Vec<UpgradeInfo>`

- [ ] **Step 1: `models/software.rs` 追加 `UpgradeInfo`**

在 `InstalledSoftwareList` 定义之前（或文件合适位置）追加：

```rust
/// 软件升级检测结果：target_version 为比当前版本高的最高可用版本
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpgradeInfo {
    pub key: String,
    pub name: String,
    pub current_version: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target_version: Option<String>,
}
```

- [ ] **Step 2: `commands/software.rs` 追加 `compute_upgrades` 与 `check_upgrades` 命令**

在 `check_upgrades` 之前（或任意命令函数旁）插入：

```rust
/// 计算已装软件的升级目标：catalog 中高于当前版本的最高版本
fn compute_upgrades(installed: &[InstalledSoftware], catalog: &Catalog) -> Vec<crate::models::software::UpgradeInfo> {
    use crate::models::software::UpgradeInfo;
    let mut out = Vec::new();
    for sw in installed {
        if sw.is_custom {
            continue;
        }
        let Some(entry) = catalog.entries.iter().find(|e| e.key == sw.key) else {
            continue;
        };
        let target = entry
            .versions
            .iter()
            .map(|v| v.version.as_str())
            .filter(|v| compare_versions(v, &sw.version) == std::cmp::Ordering::Greater)
            .max_by(|x, y| compare_versions(x, y))
            .map(|s| s.to_string());
        out.push(UpgradeInfo {
            key: sw.key.clone(),
            name: entry.name.clone(),
            current_version: sw.version.clone(),
            target_version: target,
        });
    }
    out
}

/// 升级检测：基于当前 catalog（含内置 + 远程合并缓存）纯本地对比，立即返回
#[tauri::command]
pub fn check_upgrades(manager: State<'_, Arc<SoftwareManager>>) -> Vec<crate::models::software::UpgradeInfo> {
    let catalog = manager.get_catalog();
    let installed = manager.get_installed();
    compute_upgrades(&installed, &catalog)
}
```

确认 `commands/software.rs` 顶部已 `use crate::services::software_manager::SoftwareManager;`（现有文件已导入）。
顺带在 `mod tests`（Task 1 加的）里追加 `compute_upgrades` 测试：

```rust
    use crate::models::software::{Catalog, CatalogEntry, CatalogVersion, InstalledSoftware, InstalledSoftwareList, InstallSource, SoftwareCategory, SoftwareStatus};
    use crate::services::software_manager::SoftwareManager;

    fn dummy_installed(key: &str, version: &str) -> InstalledSoftware {
        InstalledSoftware {
            id: format!("{}-{version}", key),
            key: key.into(),
            version: version.into(),
            name: format!("{key} {version}"),
            install_path: format!("{key}/{version}"),
            install_time: chrono::Utc::now().naive_utc(),
            status: SoftwareStatus::Unknown,
            port: 0,
            config: serde_json::json!({}),
            is_custom: false,
            auto_start_on_app_start: false,
            startup_order: 0,
            source: InstallSource::Mirror { mirror_name: "m".into(), url: "http://x".into() },
            pid: None,
            last_started_at: None,
            last_stopped_at: None,
            last_error: None,
            custom_start_command: None,
            category: None,
        }
    }

    fn dummy_catalog(key: &str, versions: &[&str]) -> Catalog {
        Catalog {
            updated_at: None,
            entries: vec![CatalogEntry {
                key: key.into(),
                name: key.to_uppercase(),
                description: String::new(),
                description_i18n: None,
                category: SoftwareCategory::Database,
                icon: String::new(),
                versions: versions
                    .iter()
                    .map(|v| CatalogVersion {
                        version: v.to_string(),
                        mirrors: vec![],
                        archive: crate::models::software::ArchiveInfo { format: crate::models::software::ArchiveFormat::Zip, size: None, sha256: None },
                    })
                    .collect(),
                default_version: versions[0].to_string(),
            }],
        }
    }

    #[test]
    fn compute_upgrades_picks_highest() {
        let installed = vec![dummy_installed("mysql", "5.7.44")];
        let catalog = dummy_catalog("mysql", &["5.7.44", "8.0.36", "8.4.0"]);
        let ups = super::compute_upgrades(&installed, &catalog);
        assert_eq!(ups.len(), 1);
        assert_eq!(ups[0].target_version.as_deref(), Some("8.4.0"));
    }

    #[test]
    fn compute_upgrades_none_when_latest() {
        let installed = vec![dummy_installed("mysql", "8.4.0")];
        let catalog = dummy_catalog("mysql", &["5.7.44", "8.0.36", "8.4.0"]);
        let ups = super::compute_upgrades(&installed, &catalog);
        assert_eq!(ups[0].target_version, None);
    }
```

- [ ] **Step 3: `lib.rs` 注册命令**

在 `src-tauri/src/lib.rs` 的 `commands::software::fetch_remote_versions_for`（约 227 行）之后插入：

```rust
            commands::software::check_upgrades,
```

- [ ] **Step 4: 运行测试**

Run: `cd /d/object/opx/src-tauri && cargo test --lib 2>&1 | tail -8`
Expected: 全部 ok（新增 `compute_upgrades_*` 两个用例 + Task 1 共 7 个 software 测试）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/models/software.rs src-tauri/src/commands/software.rs src-tauri/src/lib.rs
git commit -m "feat(software): check_upgrades 命令按 catalog 对比返回可升级目标"
```

---

### Task 3: 前端升级检测 + 升级动作（SoftwareListPage）

**Files:**
- Modify: `src/models/software.ts`（追加 `UpgradeInfo`）
- Modify: `src/modules/software-manager/pages/SoftwareListPage.vue`

**Interfaces:**
- Consumes: `check_upgrades`、`fetch_remote_versions_for`、`install_software`（均已有）、`installStore`（`createTask`/`initEvents`）、`SoftwareInstanceRow` 的 `upgrade` 事件与 `upgrade-to` prop（Task 4 定义）
- Produces: 页面级 `upgradeMap: Record<string, string>`（key → 目标版本）

- [ ] **Step 1: `src/models/software.ts` 追加类型**

在 `InstalledSoftwareList` 附近追加：

```ts
export interface UpgradeInfo {
  key: string
  name: string
  current_version: string
  target_version?: string | null
}
```

- [ ] **Step 2: `SoftwareListPage.vue` 接入检测与升级**

a) `script setup` 顶部 import 增加：

```ts
import InstallProgressDialog from '../components/InstallProgressDialog.vue'
import { useInstallStore } from '../stores/install'
import type { UpgradeInfo } from '@/models/software'
```

b) 增加 state 与逻辑（放在 `const actingStates` 附近）：

```ts
const installStore = useInstallStore()
// key → 目标升级版本（无可升级则无该 key）
const upgradeMap = ref<Record<string, string>>({})

function applyUpgrades(list: UpgradeInfo[]) {
  const map: Record<string, string> = {}
  for (const u of list) {
    if (u.target_version) map[u.key] = u.target_version
  }
  upgradeMap.value = map
}

/** 内置检测先行返回；随后并行在线刷新，失败静默回退 */
async function loadUpgrades() {
  try {
    const list = await invoke<UpgradeInfo[]>('check_upgrades')
    applyUpgrades(list)
    refreshUpgrades(list.map((u) => u.key))
  } catch (e) {
    console.error('check_upgrades failed:', e)
  }
}

async function refreshUpgrades(keys: string[]) {
  const uniq = [...new Set(keys)]
  await Promise.allSettled(
    uniq.map((k) => invoke('fetch_remote_versions_for', { key: k })),
  )
  try {
    applyUpgrades(await invoke<UpgradeInfo[]>('check_upgrades'))
  } catch (e) {
    console.error('refresh upgrades failed:', e)
  }
}

/** 一键升级：安装 catalog 中的目标版本（新旧并存，不自动启动） */
async function onUpgrade(item: InstalledSoftware) {
  const target = upgradeMap.value[item.key]
  if (!target) return
  try {
    const installId = (await invoke('install_software', {
      params: { key: item.key, version: target, mirror_index: 0, set_as_default_jre: false },
    })) as string
    installStore.createTask(installId, item.key, `${item.name} → ${target}`)
  } catch (e) {
    console.error('upgrade failed:', e)
  }
}
```

c) `onMounted` 增加 initEvents 与 loadUpgrades（保持 `loadInstalled` 先执行）：

```ts
onMounted(async () => {
  await lifecycleStore.initListener()
  await installStore.initEvents()
  await loadInstalled()
  loadUpgrades()
  pollTimer = setInterval(loadInstalled, 30_000)
})
```

d) 模板中给行组件传 `upgrade-to` 并监听 `upgrade`：

```html
<SoftwareInstanceRow
  v-for="item in group.items"
  :key="item.id"
  :software="mergeStatus(item)"
  :acting-states="actingStates"
  :upgrade-to="upgradeMap[item.key]"
  @start="onStart(item)"
  @stop="onStop(item)"
  @config="onConfig(item)"
  @startup-settings="onStartupSettings(item)"
  @uninstall="onUninstall(item)"
  @log="onLog(item)"
  @backup="onBackup(item)"
  @reset="onReset(item)"
  @upgrade="onUpgrade(item)"
/>
```

e) 模板 Dialog 区追加安装进度面板（放在 `BackupRestoreDialog` 之后）：

```html
    <InstallProgressDialog />
```

- [ ] **Step 3: 提交（与 Task 4 同步完成后提交，见 Task 4 Step 3）**

---

### Task 4: 行内「可升级」徽标（SoftwareInstanceRow）

**Files:**
- Modify: `src/modules/software-manager/components/SoftwareInstanceRow.vue`

**Interfaces:**
- Consumes: prop `upgradeTo?: string | null`（父页面按 key 传入）
- Produces: emits `upgrade: []`

- [ ] **Step 1: 组件增加 prop / emit / 按钮**

在 `defineProps` 增加：

```ts
const props = defineProps<{
  software: InstalledSoftware
  actingStates?: Record<string, 'start' | 'stop'>
  upgradeTo?: string | null
}>()
```

在 `defineEmits` 增加：

```ts
  upgrade: []
```

在 `card-actions` 开头（start 按钮前）加升级按钮（模板中 props 自动解包，直接用 `actingStates`/`software`）：

```html
      <button
        v-if="upgradeTo"
        class="btn primary"
        :disabled="actingStates?.[software.id] != null"
        :title="t('upgradeHint')"
        @click="$emit('upgrade')"
      >
        <Icon icon="mdi:package-upgrade" /> {{ $t('upgradeTo', { v: upgradeTo }) }}
      </button>
```

- [ ] **Step 2: 提交（含 Task 3 前端改动）**

```bash
git add src/models/software.ts src/modules/software-manager/pages/SoftwareListPage.vue src/modules/software-manager/components/SoftwareInstanceRow.vue
git commit -m "feat(software): 已装列表行内显示可升级并一键升级"
```

---

### Task 5: i18n 文案 + 最终验证

**Files:**
- Modify: `src/locales/zh-CN.ts`
- Modify: `src/locales/en-US.ts`

- [ ] **Step 1: 补文案**

`zh-CN.ts` 的软件管理段落追加：

```ts
  upgradeTo: '升级到 {v}',
  upgradeHint: '安装 catalog 中的更高版本（新旧并存，不自动启动）',
```

`en-US.ts` 对应追加：

```ts
  upgradeTo: 'Upgrade to {v}',
  upgradeHint: 'Install a newer version from catalog (keeps old version, does not auto-start)',
```

- [ ] **Step 2: 验证前端类型 + Rust 全量测试**

Run: `cd /d/object/opx && npx vue-tsc --noEmit 2>&1 | tail -10`
Expected: 无类型错误

Run: `cd /d/object/opx/src-tauri && cargo test --lib 2>&1 | tail -6`
Expected: 全部 ok（含 compare_versions + compute_upgrades 共 7 个新测试）

- [ ] **Step 3: 提交**

```bash
git add src/locales/zh-CN.ts src/locales/en-US.ts
git commit -m "feat(software): 升级检测 i18n 文案"
```